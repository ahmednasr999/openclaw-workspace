# Candidate A: Independent Verification for Derived Claims

Status: non-active proposal artifact. This file is evidence for governed replay; it does not change NASR behavior.

Exact proposed target: `AGENTS.md`, under `## Core Contract`.

Exact bounded edit (one new rule):

> For a material claim derived from a database, memory, or pipeline, do not treat the producing query or path as verification. Re-derive it through a different query, key, grouping, or authoritative source; require explicit evidence for relationship claims; and correct, hedge, or remove any material claim that cannot be independently verified before delivery.

Why this target: the control applies across reporting, briefing, content, job, and knowledge workflows. A single concise execution rule avoids duplicating the same requirement across multiple skills.

Promotion boundary: replay acceptance is not approval. `AGENTS.md` must remain unchanged until Ahmed approves this exact target and text in a separate promotion step.
