# Core Files v2 Drafts

Draft replacements for root instruction/context files.

These are tracked here to avoid root-level untracked hygiene alerts while preserving the drafts for explicit review before any promotion to live root files.

Do not promote these over `AGENTS.md`, `SOUL.md`, `TOOLS.md`, or `USER.md` without deliberate review and approval, because those files affect agent behavior.
