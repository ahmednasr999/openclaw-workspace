# Daily Notes - 2026-04-28

# Daily Notes - 2026-04-28

## OpenClaw update, runtime patches, and latency cleanup
- Ahmed approved an OpenClaw update and careful post-update verification on 2026-04-28.
- OpenClaw was updated from `2026.4.25` to `2026.4.26 (be8c246)` using `/usr/bin/npm i -g openclaw@latest --no-fund --no-audit --loglevel=error` after backup.
- Backup archive created: `/root/.openclaw/backups/manual-update-20260428/openclaw-2026-04-28_1555.tar.gz` (~6.5G). Patch backup dir: `/root/.openclaw/backups/manual-update-20260428/patches-20260428-160308`.
- Update overwrote runtime patches; they were restored and verified afterward:
  - session-resume fallback prefix suppressed
  - active-memory direct FTS live-reply patch present
  - runtime-context custom-message queue disabled
  - runtime-context plain-header leak stripper present
  - runtime-context sanitizer smoke passed
- Checker script of interest: `/root/.openclaw/workspace/scripts/check-openclaw-runtime-patches.py`.
- Gateway was later verified running on loopback `127.0.0.1:18789`, PID `173032`, package `OpenClaw 2026.4.26 (be8c246)`.
- Active-memory direct FTS was live in production, with logs showing `directFts=1`; measured active-memory latency: n=59, min 2ms, median 6ms, p90 15ms, max 59ms.
- Performance validation artifacts were created under `/tmp/openclaw-perf-validate-20260428-171444`; `/tmp/openclaw-perf-validate-latest` pointed there.
- Measured local command timings after update/patches:
  - `openclaw --version`: median ~0.070s
  - `openclaw config validate`: median ~3.35s
  - `openclaw gateway status`: median ~1.63s
  - `openclaw status`: ~48-56s, still very slow
- Practical conclusion shared with Ahmed: OpenClaw internals and patched active-memory are fast, but end-to-end Telegram replies remain dominated by model/provider latency, huge active session context, LCM/deferred compaction debt, heavy CLI/status paths, and occasional stuck-session noise.
- Ahmed approved moving ahead with session/context slimming and model-path latency testing after this diagnosis.
- Started low-risk latency cleanup: reduced obvious context bloat knobs, validated config successfully, attempted a soft reload, then moved toward controlled gateway/systemd restart and transcript/context cleanup with backup first. Final post-cleanup verification still needs to be completed after compaction/resume.

## JobZoom AI Scoring Engine warning fix
- Ahmed challenged why JobZoom kept showing `AI Scoring Engine: WARNING` for 2 consecutive days when batch scoring had actually succeeded.
- Verified 2026-04-28 JobZoom run in `/root/.openclaw/workspace-jobzoom`:
  - 150 searches, 150 successful, 0 failed
  - 359 scraped jobs
  - 341 eligible after exclusions
  - 264 after dedup
  - 70 after Pass 1
  - 7 after Pass 2 at threshold 70+
  - 7 CVs generated and report delivered
  - PDF summary: `Today: 7 matches | Yesterday: 2 ↑ | This week total: 9 | This month total: 140`
- Confirmed warning cause was only a pre-scoring health-check timeout, not quota or scoring failure:
  - one failed `scoring_health_check` due to local gateway `ReadTimeout` on `127.0.0.1:18789`
  - seven successful `batch_scoring` calls afterward, all HTTP 200
- Patched `/root/.openclaw/workspace-jobzoom/scripts/daily_run.py` after Ahmed explicitly asked to fix the repeated warning noise.
- Backup created: `/root/.openclaw/workspace-jobzoom/scripts/daily_run.py.fix-ai-status-20260428091911.bak`.
- Patch added scoring-call summary logic and AI-health decision logic so successful batch scoring with all pass1 jobs scored is reported as `HEALTHY`, even if the pre-check timed out. Health-check timeout is retained as an explanatory note, not a warning, when live batch scoring succeeds.
- Important rule reaffirmed: JobZoom is protected; do not modify JobZoom files/artifacts unless the current task is explicitly JobZoom-related or Ahmed approves.

## VPS disk cleanup
- Ahmed asked to carefully clean VPS disk until complete.
- Before cleanup: root filesystem `/dev/sda1` 96G total, 72G used, 25G free, 75%; inodes 13% used.
- Actions taken:
  - Removed low-risk rebuildable caches: npm cache, selected `/root/.cache` package/browser/temp caches, old `/tmp` temp artifacts.
  - Removed stale `/root/openclaw-staging-2026.4.20` directory.
  - Removed old rebuildable plugin runtime deps caches, keeping current `openclaw-2026.4.26-4eca5026e977`.
  - Compressed large raw LCM backup DBs with zstd:
    - `/root/.openclaw/backups/context-pressure-20260428193042/lcm.db` -> `lcm.db.zst` (~399M)
    - `/root/.openclaw/backups/lcm-perf-tune-20260428-174520/lcm.db.bak` -> `lcm.db.bak.zst` (~398M)
  - Removed superseded `/root/.openclaw/backups/pre-openclaw-update-20260427-155917` because today's full manual update archive remains retained.
- Preserved today's full update backup: `/root/.openclaw/backups/manual-update-20260428/openclaw-2026-04-28_1555.tar.gz` (~6.5G).
- After cleanup: root filesystem 96G total, 62G used, 35G free, 65%; inodes 11% used; `/tmp` 3.0G free.
- Verification: `openclaw --version` showed `OpenClaw 2026.4.26 (be8c246)`, config valid, runtime patch checker OK, gateway running PID 176254 on 127.0.0.1:18789. Health dashboard WARN only for recent stuck-session diagnostics, not disk/config/runtime patches.
- Cleanup log: `/root/.openclaw/workspace/reports/disk-cleanup-20260428.log`.

## VPS disk cleanup - extra safe pass
- Ahmed asked whether more safe cleanup was possible; completed an additional conservative pass.
- Before extra pass: root filesystem 96G total, 58-62G used range after first pass, 35G free, ~65% full.
- Extra cleanup actions:
  - Ran `pnpm store prune` to remove unused cached packages/metadata.
  - Removed NVM downloaded-archive cache only, not the installed Node version.
  - Removed rebuildable `/root/openclaw` source artifacts: `dist`, `dist-runtime`, `.local`, `.artifacts`; kept source repo, `.git`, and `node_modules`.
  - Removed non-Linux Camoufox font caches only (`macos`, `windows`); kept Linux cache and all browser profiles/cookies.
  - Removed redownloadable Whisper model cache `/root/.cache/whisper/small.pt`.
  - Cleared small node/opencode caches.
- Final state: `/dev/sda1` 96G total, 58G used, 39G free, 60%; inodes 9% used; `/tmp` 3.0G free.
- Verification: OpenClaw `2026.4.26 (be8c246)`, config valid, runtime patches OK, gateway PID 176254 reachable on loopback. Health dashboard WARN remained only for context/stuck-session diagnostics, not cleanup damage.
- Preserved: current OpenClaw runtime, live config/state, all agent workspaces, browser profiles/cookies, media, and today's full manual update backup.
- Extra cleanup log: `/root/.openclaw/workspace/reports/disk-cleanup-20260428-extra.log`.

## JobZoom product BRD created
- Ahmed asked for a full detailed BRD for JobZoom so an AI builder can understand the product A-to-Z.
- Created source BRD Markdown: `/root/.openclaw/workspace/product-specs/jobzoom-brd.md` (1332 lines, ~31K).
- Created DOCX artifact: `/root/.openclaw/workspace/output/doc/JobZoom-BRD.docx`.
- Created HTML artifact: `/root/.openclaw/workspace/output/doc/JobZoom-BRD.html`.
- PDF conversion via LibreOffice failed with `Error: source file could not be loaded`, but the DOCX zip integrity tested OK. If PDF is needed, retry via another renderer or inspect LibreOffice issue.
- BRD includes executive summary, business objectives, scope, target market, pricing, business rules, functional/non-functional requirements, data model, process flows, report/email/admin/security/privacy/legal requirements, QA, acceptance criteria, risks, launch plan, and AI-builder prompt.

## Gulf jobs scanner Exa outage fallback
- Heartbeat follow-up on active task: today's Gulf jobs scanner failed because Composio `EXA_SEARCH` returned HTTP 402 `NO_MORE_CREDITS`.
- Added a credit-safe fallback to `/root/.openclaw/workspace/scripts/linkedin-gulf-jobs.py`:
  - Exa and LinkedIn search paths now retry through `openclaw infer web search --provider duckduckgo --json` when they return no jobs.
  - The scanner can continue fallback-only if the Composio key or MCP initialization is unavailable.
  - Fallback is configurable with `GULF_JOBS_WEB_FALLBACK`, `GULF_JOBS_WEB_PROVIDER`, `GULF_JOBS_WEB_RESULTS`, and `GULF_JOBS_WEB_TIMEOUT`.
  - Reports and detailed logs now mention the active fallback provider.
- Verification completed: `python3 -m py_compile scripts/linkedin-gulf-jobs.py` passed, and direct fallback function calls returned Riyadh CTO results via DuckDuckGo/OpenClaw web search.
- Remaining next step: rerun the full scanner or let the next scheduled run verify end-to-end output; Exa credits should still be topped up for better neural search quality.
- Follow-up heartbeat hardening: added a Composio/Exa credit-exhaustion circuit breaker to `scripts/linkedin-gulf-jobs.py`. After an HTTP 402 or `NO_MORE_CREDITS` signal, subsequent Exa/MCP search calls are skipped and the scanner goes straight to the OpenClaw web fallback, avoiding repeated failed paid-provider calls. Verified with `python3 -m py_compile` and a targeted monkeypatch assertion that `exa_search`/`composio_web_search` return without calling MCP after the breaker trips.
