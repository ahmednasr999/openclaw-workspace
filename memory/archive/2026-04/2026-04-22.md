# Daily Notes - 2026-04-22

## Durable updates
- Memory plugin diagnosis corrected: the main slot was not the real blocker. `plugins.slots.memory` was already on `memory-core`; the actual break was `plugins.entries.memory-core.enabled = false`. Re-enabling that removed the doctor error `No active memory plugin is registered for the current config`.
- Later same-day verification showed the live memory-search path is now healthy again. Current state: `agents.defaults.memorySearch.provider = local`, `fallback = none`, embeddings ready, vector search ready.
- Earlier Ollama-related failures and the missing `task-registry.maintenance-*.js` chunk should be treated as transient same-day incidents, not the current live state.
- Current verified gateway state after recovery: gateway healthy on loopback, memory search healthy in the main workspace, and side workspaces `cto`, `cmo`, and `jobzoom` still need indexing cleanup rather than provider repair.
- Important correction for future reasoning: do not blame `plugins.slots.memory` first when doctor says no active memory plugin is registered. Check `plugins.entries.memory-core.enabled` and memory-search provider readiness separately, then verify the live state before repeating an older diagnosis.
# Daily Notes - 2026-04-22

## Durable updates
- Memory plugin diagnosis corrected: the main slot was not the real blocker. `plugins.slots.memory` was already on `memory-core`; the actual break was `plugins.entries.memory-core.enabled = false`. Re-enabling that removed the doctor error `No active memory plugin is registered for the current config`.
- Later same-day verification showed the live memory-search path is now healthy again. Current state: `agents.defaults.memorySearch.provider = local`, `fallback = none`, embeddings ready, vector search ready.
- Earlier Ollama-related failures and the missing `task-registry.maintenance-*.js` chunk should be treated as transient same-day incidents, not the current live state.
- Current verified gateway state after recovery: gateway healthy on loopback, memory search healthy in the main workspace, and side workspaces `cto`, `cmo`, and `jobzoom` still need indexing cleanup rather than provider repair.
- Important correction for future reasoning: do not blame `plugins.slots.memory` first when doctor says no active memory plugin is registered. Check `plugins.entries.memory-core.enabled` and memory-search provider readiness separately, then verify the live state before repeating an older diagnosis.

## Additional durable updates
- The user-visible filler reply `Nothing to add right now.` was traced to the outbound silent-reply rewrite layer, not to the model, followup runner, or `routeReply` inventing content. The critical rewrite happens in outbound planning when an exact preserved `NO_REPLY` reaches `createOutboundPayloadPlan(...)` under a direct-session silent policy with rewrite enabled.
- Surgical source fix was applied at the correct boundary: `ReplyPayload` now carries `preserveSilentToken`, `route-reply.ts` marks intentionally preserved exact `NO_REPLY`, and `src/infra/outbound/payloads.ts` respects that marker so preserved direct or queued follow-up silent payloads are kept as exact `NO_REPLY` instead of being rewritten into filler.
- Durable behavioral rule after the fix: true silent replies still drop when policy allows silence, but intentionally preserved exact `NO_REPLY` used for queued/direct follow-up handling must not be converted into Ahmed-facing filler text.
- Verification that should be remembered: targeted auto-reply scoped tests passed after the patch, and a direct planner/runtime probe confirmed a payload with `{ text: "NO_REPLY", preserveSilentToken: true }` stays exact `NO_REPLY` under direct rewrite-enabled policy. Remaining infra-scoped Vitest friction was due to local config/path narrowing behavior, not a demonstrated failing assertion.
- Related debugging lesson: queued-message replay/wrapper behavior may still need separate runtime cleanup, but the visible filler symptom itself was specifically an outbound silent-reply rewrite bug, so future debugging should not conflate the two.

## Handover - lossless-claw 0.9.2 upgrade confirmed
- `@martian-engineering/lossless-claw` was upgraded from 0.8.2 to 0.9.2 with `openclaw plugins install @martian-engineering/lossless-claw@0.9.2 --force --pin`; gateway restart was clean and the plugin now loads as 0.9.2 without `info.id must match` errors.
- Verified durable fix state: upstream PR #444 is present in the shipped artifact, `dist/index.js` now natively reports `id:"lossless-claw"`, and the old local sed hotfix is no longer needed.
- Migration/engine state after upgrade was healthy: migration ran clean in 243ms, engine initialized, config preserved, and earlier local fixes remained intact, including `agents.defaults.silentReplyRewrite.direct = false` and `plugins.entries.active-memory.config.modelFallback`.
- Pre-upgrade rollback snapshot exists at `/root/.openclaw/backups/lossless-claw-pre-092-upgrade/`; rollback steps are documented in runbook section 10.1.
- Lossless issue #427 monitor cron `adf135b8` was disabled because the shipped plugin fix makes the watcher obsolete; the job remains only as historical record.
- Runbooks were updated to mark the old mismatch workaround as historical, point recovery to reinstalling 0.9.2+, note that reinstalls of 0.9.2+ are safe, and record backup files as `*.bak-pre-092-20260422T231423`.
- Important status correction: issue #427 is still formally open on GitHub even though the fix shipped; the reliable claim is "fixed in plugin 0.9.2+, issue may still appear open upstream."
- Two unrelated uncommitted files remain intentionally untouched in `/root/openclaw`: `session-reset-prompt.ts` and `session-reset-prompt.test.ts`.
