# Daily Notes - 2026-04-11

# Daily Notes - 2026-04-11

## 2026-04-11 - Gateway/model/session cleanup
- Fixed active agent session overrides so the live Telegram/direct routes for main, HR, CTO, CMO, and JobZoom are aligned to `openai-codex/gpt-5.4` with `thinkingLevel: high` and `providerOverride: openai-codex`.
- Cleaned stale session metadata and archived old inactive Telegram/session records; archived orphan main-session transcripts and removed broken `sessions.json` entries referencing missing files.
- Patched `/root/.openclaw/openclaw.json` so `models.providers.openai-codex` now uses native Codex transport: `baseUrl=https://chatgpt.com/backend-api`, `api=openai-codex-responses`.
- Moved stale `gpt-5.4-pro` references off Codex to native OpenAI path: alias `gpt54pro` and two Slack channel overrides now use `openai/gpt-5.4-pro`.
- Pinned explicit heartbeat `directPolicy` values: global/default + main = `allow`; HR/CTO/CMO/JobZoom = `block`.
- Added `tools.exec.safeBinTrustedDirs` for `/root/.nvm/versions/node/v22.22.0/bin` and `/usr/local/bin` to clear trusted-bin path noise without broadening allowed commands.
- Archived abandoned duplicate state dir `/home/bridge/.openclaw` to `/home/bridge/.openclaw.archive-20260411T133348Z`; doctor duplicate-state-dir warning cleared.

## 2026-04-11 - Gateway hardening completed
- Reloaded user systemd and restarted `openclaw-gateway.service`; live service now runs `/usr/bin/node /root/openclaw/dist/index.js gateway --port 18789` with `PATH=/usr/local/bin:/usr/bin:/bin`.
- Rewrote `/root/.config/systemd/user/openclaw-gateway.service` to the stable 2026.4.9 runtime and reduced the drop-in override to the Telegram family setting only.
- Backed up the old `/usr/bin/openclaw` target to `tmp/usr-bin-openclaw-link-backup-20260411T1558Z.txt`, then repointed `/usr/bin/openclaw` to `/root/openclaw/openclaw.mjs` so plain shell `openclaw` matches the live gateway runtime.
- Verified `openclaw --version` now reports `OpenClaw 2026.4.9 (37fb1eb)` and `openclaw doctor --non-interactive` exits `0` with the version-manager PATH, version-manager Node, entrypoint mismatch, duplicate state dir, and trusted-bin warning cluster cleared.
