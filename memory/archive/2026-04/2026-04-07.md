# Daily Notes - 2026-04-07

# Daily Notes - 2026-04-07

## Durable memory flush
- Ahmed set durable model policy: GPT-5.4 via OpenAI Codex OAuth is primary going forward, MiniMax-M2.7 is fallback only, all agents should default to GPT-5.4, Claude/Sonnet/Opus defaults/references should be removed where they drive behavior, and any model switch must be disclosed immediately in the next reply.
- `/root/.openclaw/workspace/AGENTS.md` and `/root/.openclaw/workspace/SOUL.md` were updated earlier today to reflect GPT-5.4 as default and MiniMax-M2.7 as fallback.
- Health check established global config alignment: defaults.model.primary set to `openai-codex/gpt-5.4`, all four agents and all heartbeat models configured to GPT-5.4.
- `/root/.openclaw/cron/jobs.json` was updated to change 20 enabled cron job `payload.model` pins from legacy Claude/MiniMax values to `openai-codex/gpt-5.4`. Verification after edit showed `REMAINING_NON_GPT54 0` for enabled jobs with explicit model pins.
- Earlier suspected cron failures for `LCM Midday Compaction Sweep` and `SAYYAD — Score & Curate (midday)` were superseded by direct inspection of `/root/.openclaw/cron/jobs.json`; both were actually `ok`, `consecutiveErrors: 0`, with delivered recent runs.
- Gateway spam investigation found duplicate gateway service registration: both the user systemd unit `~/.config/systemd/user/openclaw-gateway.service` and the system unit `/etc/systemd/system/openclaw-gateway.service` were active and enabled at the same time. This was identified as the likely source of repeated warnings: `gateway already running under systemd; waiting 5000ms before retrying startup`.
- Ahmed approved the surgical fix plan to disable the user-level gateway service and leave the system-level gateway service as the single source of truth. Execution/verification outcome was not captured before compaction, so this remains an in-progress item to verify next session.
- OpenAI Codex OAuth token expiry remained unresolved during the health check; no expiry timestamp was found in available outputs.

## 2026-04-07 - System baseline reset
- Ahmed confirmed all-clear and set the new operating baseline.
- Primary model for all agents is `openai-codex/gpt-5.4`; fallback is `minimax-portal/MiniMax-M2.7`.
- No Anthropic, Moonshot, or OpenRouter free tier in active stack.
- Fixed today: cron model repins, duplicate user gateway disablement, Tavily env cleanup, AGENTS/TOOLS bootstrap trimming, GPT-5.4 policy updates, auth cooldown clear, gateway spam eliminated.
- Watchlist: Codex OAuth expiry/autorefresh, unexpected MiniMax fallback, Slack pong timeouts only if message delivery impact, stale plugins.allow entries.
- Standing order: disclose any fallback in first line, resume normal operations, run self-health-check every Sunday morning and post to CEO General thread.

