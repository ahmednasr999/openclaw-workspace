# 2026-05-03 Durable Session Notes

- LinkedIn publish recovery: approved post about AI agents needing systems was published twice due to a recovery race between CMO/subagent and main session.
  - First/keep candidate: `https://www.linkedin.com/feed/update/urn:li:share:7456440658388688896` published/logged by CMO/subagent around 2026-05-02 23:33 Cairo.
  - Second/duplicate candidate: `https://www.linkedin.com/feed/update/urn:li:share:7456442093582954496/` published by main session around 2026-05-02 23:38 Cairo.
  - Recommendation given to Ahmed: delete the second post and keep the first. No deletion was performed because deletion is public/destructive and requires explicit approval.
  - Durable lesson: before any external publish recovery, re-check local success logs/live state immediately before the final `LINKEDIN_CREATE_LINKED_IN_POST` call to prevent duplicates.
- Composio LinkedIn image-post lane is proven end-to-end:
  1. stage approved image to `openclaw-workspace` branch `images-latest` under `images/posts/...`,
  2. verify raw GitHub URL returns the correct bytes/hash,
  3. download raw URL inside Composio workbench session,
  4. call workbench `upload_local_file()` to obtain `images[].s3key`,
  5. call `LINKEDIN_CREATE_LINKED_IN_POST` with `author: urn:li:person:mm8EyA56mj`, `visibility: PUBLIC`, approved commentary, and image object `{name, mimetype, s3key}`.
  - Confirmed raw URL for this incident: `https://raw.githubusercontent.com/ahmednasr999/openclaw-workspace/images-latest/images/posts/ai-agents-need-a-system-stack.jpg`
  - Confirmed image sha256: `2c6f8c4fde3f8a6b33d77ecb0aca5eb580eaf5f608455a2c205c752835ba3a20`
  - Main-session S3 key used: `project/pr_Gqp3256AsBat/tool_router_session/trs_gNwouOSOw0Ze/G0ntv4Jrxiwb`
- Important pitfall: `COMPOSIO_REMOTE_WORKBENCH` requires Python `code_to_execute`; it is not an `{action:"upload_local_file", path:...}` wrapper. The file must already exist inside the remote sandbox before `upload_local_file(path)` can return a valid `s3key`.
- Browser fallback remains unsuitable for LinkedIn publishing unless authenticated: imported LinkedIn cookies redirected to login/password, and Ahmed-Mac relay was disconnected during the incident.

- JobZoom daily scan logic was patched in `/root/.openclaw/workspace-jobzoom/scripts/daily_run.py` after discovering a design flaw: older-but-still-open roles returned by today's LinkedIn scrape were being silently dropped by the strict freshness gate before scoring/saving.
  - Backup: `/root/.openclaw/workspace-jobzoom/scripts/daily_run.py.bak-still-open-20260503`.
  - New lane preserves strict fresh matching while adding a separate still-open review lane for roles older than yesterday but within `STILL_OPEN_LOOKBACK_DAYS = 14`, excluding already-applied roles.
  - New constants: `STILL_OPEN_LOOKBACK_DAYS = 14`, `STILL_OPEN_SCORE_THRESHOLD = 70`, `STILL_OPEN_MAX_AI_SCORE = 20`, `STILL_OPEN_MAX_REPORT = 12`.
  - Validation on current scrape data showed: fresh eligible 11, still-open raw candidates 350, after applied exclusions 305, expanded review filter 206. Future runs should AI-score top 20 and report separately; no auto-CV generation yet.
  - Durable lesson: for probabilistic LinkedIn search, do not equate posted-date freshness with discovery-date freshness. If a role appears in today's scrape, strong older-but-open roles need a bounded review lane instead of being discarded silently.

- OpenClaw update completed on 2026-05-03 after explicit approval.
  - Pre-update backup: `/root/.openclaw/backups/pre-openclaw-update-20260503-060358`.
  - Installed package update target was `2026.5.2`; active package root `/usr/lib/node_modules/openclaw`.
  - Corrected update path forced `/usr/bin/openclaw` because `/usr/local/bin/openclaw` points to stale dirty checkout `/root/openclaw` (`2026.4.22`), while `/usr/bin/openclaw` points to package install (`2026.4.29` before update, `2026.5.2` after update).
  - Successful update log: `/root/.openclaw/workspace/tmp/openclaw-controlled-update-package.log`; helper script: `/root/.openclaw/workspace/tmp/openclaw-controlled-update-package.sh`.
  - First attempted update log/script: `/root/.openclaw/workspace/tmp/openclaw-controlled-update.log` and `.sh`; it hit the wrong `/root/openclaw` checkout due to PATH precedence and skipped because the checkout was dirty.
  - Final active version after update: `OpenClaw 2026.5.2 (8b2a6e5)`, gateway enabled/running under systemd, loopback `127.0.0.1:18789`, admin-capable.
  - Durable lesson: detached/systemd jobs must force `/usr/bin` before `/usr/local/bin` or use an absolute CLI path for package updates, otherwise stale source checkout can hijack OpenClaw commands.

- Post-update runtime sanitizer patches were overwritten and restored against OpenClaw `2026.5.2` dist bundles.
  - Verification source: `/root/.openclaw/workspace/scripts/check-openclaw-runtime-patches.py`.
  - Patched/restored files included:
    - `/usr/lib/node_modules/openclaw/dist/sanitize-user-facing-text-Cz177X8d.js`
    - `/usr/lib/node_modules/openclaw/dist/queue-y-VPBYRY.js`
    - `/usr/lib/node_modules/openclaw/dist/heartbeat-runner-BHI-wtwo.js`
    - `/usr/lib/node_modules/openclaw/dist/bash-tools-CUUGvp_C.js`
    - `/usr/lib/node_modules/openclaw/dist/bash-tools-CwNyLceP.js`
    - `/usr/lib/node_modules/openclaw/dist/extensions/active-memory/index.js`
    - `/usr/lib/node_modules/openclaw/dist/runtime-context-prompt-BusW75hm.js`
    - `/usr/lib/node_modules/openclaw/dist/internal-runtime-context-TysOkYqY.js`
    - `/usr/lib/node_modules/openclaw/dist/queue-helpers-CjFYraLI.js`
  - Backups created for early patched bundles with suffix `.bak-post-20260503-0618`.
  - Final checker was fully green, including: session-resume fallback prefix suppressed, active-memory direct FTS live-reply patch present, runtime-context custom-message queue disabled, runtime-context plain-header stripper/smoke passed, leaked tool-callback sanitizer smoke passed, heartbeat reply sanitizer path present, cron envelope smoke passed, queued-message metadata smoke passed, active-memory queued system smoke passed, restart-sentinel smoke passed, reply-context metadata smoke passed.
  - Runtime reload used SIGUSR1 with reason `Load restored OpenClaw runtime sanitizer patches after 2026.5.2 update`; no extra full service disruption beyond update restart was needed.
  - Durable lesson: after OpenClaw updates, duplicate hashed bundles can make patch checks fail even if the active-looking bundle is patched. Patch/check both real active bundles and stale duplicate bundles selected by checker logic, especially `bash-tools-*` and `queue-*`.

- Disk cleanup on OpenClaw VPS completed after approval.
  - Before cleanup: root filesystem `/dev/sda1` 96G total, 72G used, 25G free, 75%.
  - Main safe target: old OpenClaw snapshots in `/root`, after verifying active processes were not using them.
  - Removed `/root/openclaw-snapshot-20260501` and `/root/openclaw-snapshot-20260502`.
  - Also ran low-risk apt/npm cache cleanup and journal trim.
  - Did not touch `/root/.openclaw/lcm.db` (memory/recall state, ~2.7G), active OpenClaw workspace/state, `/root/openclaw`, browser profiles/tool caches.
  - After cleanup: 53G used, 44G free, 56%, about 19G reclaimed.

- Daily profile drip pilot format standardized after Ahmed asked how daily questions should be formatted.
  - Standard format every day:
    1. `🎯 Daily profile calibration`
    2. `Why it matters: ...` one short sentence tied to JobZoom filtering, CV positioning, interview prep, outreach, or automation behavior.
    3. `Question: ...` exactly one thoughtful question.
    4. `Reply format: ...` low-friction answer pattern such as `Top 1-2 + hard no`, `A/B/C`, `one sentence`, or `minimum number + currency`.
    5. Keep under 5 short lines; never send a bare question without context and reply format.
  - Updated `/root/.openclaw/workspace/memory/daily-profile-drip.md` and cron job `12f2b3c3-4d4e-4c7e-87bc-3e3ee9825326` to enforce the format.
  - Cron remains enabled at `15 9 * * *` in `Africa/Cairo`.
  - Ahmed answered the employer-type calibration: “The answer is the employer will pay more salary.” Durable rule: employer type is secondary; when executive roles are otherwise similar, prioritize the employer paying more salary. USER.md now reflects salary as a primary factor.

- `summarize` v0.14.0 was evaluated after Ahmed shared a release link.
  - Local binary present: `/usr/bin/summarize`, version `0.14.0 (8d97ba67)`.
  - Release highlights: built-in `gpt-fast` / `fast` presets for Codex GPT-5.5 Fast mode, Chrome extension extractor routing improvements including Reddit `.json`, local PDF `--extract` without LLM, auto-model honors config-provided env values, avoids always-on scripts on Facebook/Instagram/Meta CDN.
  - Smoke test: local PDF extraction worked on Ahmed CV PDF and produced readable content.
  - Initial web tests failed due to operator CLI misuse: bare `--markdown` was wrong because current CLI expects markdown mode/format arguments. Corrected understanding showed release content extraction worked.
  - Durable recommendation: use `summarize` selectively as secondary extractor, especially local PDF extraction and ad-hoc URL summaries; do not rebuild core workflows around it until broader representative testing.

- Lossless Claw extension updated successfully to v0.9.3 after approval.
  - Backup tarball: `/root/.openclaw/backups/lossless-claw-20260503-181728.tgz` with checksum `784641c0c91b4b769c63c9183dfd5ddf3523b38960da507fe6fbce762b193b6b`.
  - Installed package `@martian-engineering/lossless-claw` version `0.9.3`.
  - v0.9.3 manifest declares `contracts.tools`: `lcm_grep`, `lcm_describe`, `lcm_expand`, `lcm_expand_query`.
  - Important v0.9.3 changes: honors `cacheAwareCompaction.enabled:false` at deferred dispatch gate, critical-pressure escape before overflow, mutation-sensitive Codex prompt cache writes/touches, modern `openai-codex` summarization fixes, token accounting guidance fix, declared tool contracts, uncataloged provider models, ingestion/prefill/replay/migration fixes, strips reasoning/thinking blocks from leaf summarizer input.
  - Initial import smoke failed with `ERR_MODULE_NOT_FOUND` for `@mariozechner/pi-coding-agent` because v0.9.3 moved `@mariozechner/pi-agent-core`, `pi-ai`, `pi-coding-agent` to peer deps and extension-local ESM resolution could not find host runtime peers.
  - Fix: symlinked extension-local `/root/.openclaw/extensions/lossless-claw/node_modules/@mariozechner/{pi-agent-core,pi-ai,pi-coding-agent}` to `/usr/lib/node_modules/openclaw/node_modules/@mariozechner/...`.
  - After symlinks, `node -e "import('./dist/index.js')"` from the extension dir succeeded with `dist import ok`.
  - Gateway restart/reload applied updated plugin registrations. Post-restart verification: package version 0.9.3, declared 4 LCM tool contracts, gateway listening on loopback, `lossless_contract_errors=0` since current start.
  - Caveat: `openclaw lossless --help` remains unavailable because bundled CLI surface `lossless` is excluded by `plugins.allow`; this is separate from the external `lossless-claw` extension working.
  - Health WARNs during checks were LCM context pressure/maintenance backlog, not plugin failure; later health improved/OK as maintenance settled.

- Fixed unrelated plugin contract warnings for `camofox-browser` and `composio` only after confirming obvious/reversible cause.
  - Before patch, both plugin manifests lacked `contracts.tools`, while code registered tools.
  - Backups created:
    - `/root/.openclaw/extensions/camofox-browser/openclaw.plugin.json.bak-20260503-183716`
    - `/root/.openclaw/extensions/composio/openclaw.plugin.json.bak-20260503-183716`
  - Added `contracts.tools` to `camofox-browser` manifest with 10 tools: `camofox_create_tab`, `camofox_snapshot`, `camofox_screenshot`, `camofox_navigate`, `camofox_click`, `camofox_type`, `camofox_scroll`, `camofox_list_tabs`, `camofox_close_tab`, `camofox_import_cookies`.
  - Added `contracts.tools` to `composio` manifest with 7 tools: `COMPOSIO_MANAGE_CONNECTIONS`, `COMPOSIO_MULTI_EXECUTE_TOOL`, `COMPOSIO_REMOTE_BASH_TOOL`, `COMPOSIO_REMOTE_WORKBENCH`, `COMPOSIO_SEARCH_TOOLS`, `COMPOSIO_WAIT_FOR_CONNECTIONS`, `COMPOSIO_GET_TOOL_SCHEMAS`.
  - Restarted gateway. Fresh gateway status: systemd service running PID `495612`, listening on `127.0.0.1:18789`, health OK, LCM healthy with maintenance pending 0.
  - Stale aggregate log counts still showed historical warnings, but targeted current-start verification showed `contract_warnings_since_start=0`.
  - Final runtime/manifest summary: Lossless declares 4 tools, Camofox 10, Composio 7, no current-start contract warnings.

- OpenClaw docs-to-rules pass from Ahmed-shared docs links.
  - Plugin verification rule refined in `TOOLS.md`: after plugin installs/updates/manifest edits, match verification to the changed surface: `openclaw plugins inspect <plugin-id> --runtime --json` for tools/hooks/services/gateway methods; run one safe root command for plugin-owned CLI commands; `plugins list --json` only for cold manifest/config discovery; health/log greps are supporting evidence, not primary proof; check current-start logs for `plugin must declare contracts.tools` warnings.
  - Broad plugin maintenance checklist learned: treat manifest, runtime import, and runtime registration as separate gates; compare old/new manifests and compatibility metadata before update; check release notes, compatibility docs, and SDK migration notes when updates touch SDK/imports/contracts; prefer precise SDK subpath imports in custom/plugin review; check entrypoint side effects and deferred-load safety; for hooks/provider/channel/harness plugins, verify the actual behavioral surface, not just package files.
  - High-risk plugin/tool areas added to `TOOLS.md`: webhooks and voice-call plugins are high-risk external ingress and should remain disabled unless explicitly approved, with narrow session binding, strong unique secrets/allowlists, minimal tool policy, rate/timeout guards, and no external-write tools by default.
  - Codex Computer Use clarified in `TOOLS.md`: separate from Codex text/code harness routing; verify `computerUse` config, selected marketplace/backend, installed/enabled plugin, and node/backend availability before promising computer-use actions.
  - Google Meet automation rule added: privacy-sensitive; do not auto-join, record, transcribe, or summarize meetings unless Ahmed explicitly approves the workflow/session; prefer node/browser setups with known login state and narrow permissions.
  - Memory Wiki posture reinforced: conservative isolated vault, bridge disabled, URL ingest disabled, prompt digest disabled unless approved; current isolated vault should use `vault.renderMode: "obsidian"` to avoid false broken-wikilink lint warnings from native Markdown `.md` links.
  - Memory LanceDB/vector memory rule reinforced: keep vector memory in isolated benchmark/pilot lanes; do not connect to live Telegram active-memory or enable autoCapture until latency, timeout, privacy, and recall-quality checks pass.
  - Message presentation rule added: presentation blocks are optional enhancement only; important alerts/daily cards/approval prompts must remain readable as plain text if buttons/cards/selects degrade.
  - Browser docs reinforced: account/session tasks should use Ahmed-Mac Chrome when login state matters; never ask Ahmed for passwords or automate risky logins; use snapshots/refs over fragile coordinate clicks; verify CDP/profile/node layers before blaming agent/tool; avoid visual loops after repeated screenshots/scrolls without extractable content.
  - Automation docs reinforced: cron jobs live in gateway DB; cron day-of-month and day-of-week use OR logic; cron for schedules/reminders, heartbeat for proactive wakes, TaskFlow/OpenProse/Lobster only when durable orchestration/deterministic approval-gated workflow is justified; standing order content should live in durable files, cron only triggers it.
  - Exec/approval docs reinforced: approval is command-specific; preserve/show exact command/script for approval; never execute `/approve` through shell; chained/interpreter commands deserve extra scrutiny; prefer first-class tools over exec for config, messaging, cron, and gateway actions.
  - Media/docs reinforced: before promising image/video/music/TTS/PDF capability, inspect actual tool/provider availability; per-call model overrides may not fall back; generated media completion requires quality inspection, not tool success; voice/audio/media can be privacy/cost sensitive.
  - Skills docs reinforced: skills are instruction packs, not tools; read one relevant skill up front; skill availability does not prove runtime tool availability; create skills only for proven repeated workflows, keep concise, use requirements/gating where relevant.
  - Learning entries were added under `.learnings/LEARNINGS.md`; `TOOLS.md` was backed up before local governance updates.
