# CTO Pending

- Resolved 2026-08-20: cleared the reopened P1 gates that invalidated closeout `c42e50473`. Governed learning now locks baseline/candidate artifact and configuration hashes into proposals, replay packets, evaluations, and signed approval receipts; caller text no longer authenticates approval. Promotion verifies the exact receipt and signature bytes with OpenSSH namespace `openclaw-governed-learning` against the fixed external trust root `/root/.config/openclaw/governed-learning-approval-signers`, enforces owner/write permissions, detects legacy proposals, preserves approval-artifact hashes through implementation, and rejects symlinks/FIFOs without blocking. Commits: `f815906c0`, `210cfa3fc`, `be2246495`. The external signer trust root was intentionally not provisioned by this review, so promotion fails closed until a separately authorized operator installs it. YouTube capture now opens a dedicated tab, installs the requested-video XHR/fetch interceptor before endpoint-bound same-task SPA navigation, fails if navigation replaces the hooked document, uses the authenticated player's current video/track metadata, rejects other video IDs, and retains OpenClaw's stable tab handle for cleanup. Commits: `a0be70bff`, `f843bfa86` (superseding the timing-based `83dc9438b` implementation). Verification: 24 governed-learning tests, 6 YouTube assertions, Python/Node syntax, skill validation, repository preflight (20 passed, 0 failed, 5 known warnings), and a live authenticated YouTube proof that captured 61 timestamped JSON3 events for exact video ID `dQw4w9WgXcQ`. Fresh scoped GPT-5.6 Sol autoreview is clean on final governed commit `be2246495` and final YouTube commit `f843bfa86`; all accepted review findings were fixed. No runtime/config mutation or public/external write was made.
- Reopened 2026-08-20: the critical-file commits were landed concurrently before the fresh scoped GPT-5.6 Sol reviews finished, so the resolution immediately below is not a clean landing verdict. `dcf52da2b` (`USER.md`) is accepted. `d85245437` (`AGENTS.md` plus governed-learning implementation) still has two P1 governance gaps: promotion approval trusts caller-supplied `approved_by`/`approval_ref` text instead of authenticated immutable approval evidence, and replay packets are not bound to both the exact baseline and candidate artifacts/configurations. P2 follow-ups are grounded implementation evidence, serialized/atomic correction intake, and immutable/versioned candidate lifecycle state. `cc7526ea7` (`TOOLS.md` plus YouTube helper/tests) still has a P1 capture race because interception is installed only after navigation, so an already-loaded/cached caption request can be missed; P2 follow-ups are captured-track/authentication provenance, canonical source output, atomic final-file writes, `/live/` URL support, environment-qualified token wording, and browser/output-path tests. The separate untracked creator-audit skill also needs a persisted evidence-blocked state before scheduling because twice-failed IDs remain eligible for reselection. Fresh deterministic proof passed (21 Python tests, 3 Node tests, Python compilation, Node syntax, and scoped diff checks), but those checks do not prove the missing trust/evidence contracts. Next safe action: define the trusted approval and replay-attestation contract before patching `d85245437`; install interception before YouTube navigation and close the evidence-integrity gaps in `cc7526ea7`; add focused regression tests; rerun scoped autoreview on each corrective commit. Do not treat `c42e50473` as final closeout until both P1s are cleared. No runtime/config mutation was made by this review.
- Resolved 2026-08-20: reviewed and landed the critical `AGENTS.md`, `TOOLS.md`, and `USER.md` changes without staging the broader dirty tree. The correction rule now ships atomically with its governed implementation, contract, decision record, and tests in `d85245437`; review also closed candidate-only promotion, approved-artifact mismatch, and zero-cost-baseline gaps. The YouTube transcript rule ships with its executable helper and focused tests in `cc7526ea7`, including strict requested-video-ID matching to reject ad tracks. The Saudi German Health safeguard now blocks recommending, drafting, initiating, and sending career outreach unless Ahmed explicitly approves the specific contact and action, committed in `dcf52da2b`. Verification passed: 21 Python tests, 3 Node checks, Python compilation, skill validation, `git diff --check`, and repository preflight (20 passed, 0 failed, 5 known missing-file warnings). Unrelated dirty-tree files remain excluded.
- Resolved 2026-08-16: reviewed critical-diff fingerprint `e49d64f3911a5254007871898a98cdcd923af5ec51ca5196a2751f38f8663725` for `/root/.openclaw/openclaw.json`. The full nine-line repository diff is exactly the approved prompt-efficiency maintenance: remove the CMO `mode: "full"` override while preserving its Windows node, restore global `tools.exec.mode` from `full` to `auto`, and set `plugins.entries.lossless-claw.config.freshTailMaxTokens` from 12,000 to 8,000. No unknown or unapproved runtime change remains in this diff. `openclaw config validate` passed; the live Gateway probe returned `ok=true`, `degraded=false`, hot reload active, and Telegram connected. No additional config mutation or restart was made during this review.
- Resolved 2026-08-16: reviewed the prompt-efficiency cleanup in `AGENTS.md`, `SOUL.md`, and `USER.md`; all authoritative writing, media-delivery, model, approval, and LinkedIn gates remain intact. Regression checks passed and the three-file change was committed as `1322459b5` (`docs: consolidate core workspace instructions`).
- Make NASR Doctor sandbox-aware and review ingestion freshness. Its `unable to open database file` result is a cron-sandbox SQLite lock/journal artifact, not database corruption: an immutable read-only check succeeded with `integrity_check=ok`, 4,869 jobs, and 361 interactions. The latest `jobs.updated_at` is `2026-08-11 21:45:56` (about 58 hours old at the 2026-08-14 07:47 Cairo heartbeat), so the pipeline owner should review ingestion freshness. Gateway probe and Firehose/Gmail/LinkedIn failures seen inside that sandbox are non-authoritative network/permission artifacts; authoritative evidence remains a fresh Gateway log, dashboard cron last-100 `ERROR` count 0, and disk usage 69%. Preserve the immutable read-only DB fallback and skip or downgrade lock/journal and write probes when the cron sandbox cannot support them.
- Review the NASR Doctor dirty-tree batch before any commit: the 2026-08-13 07:00 check reports 410 uncommitted files, and auto-commit remains disabled pending review. Treat this as a repository-review warning, separate from runtime availability.
- Review the 2026-08-02 weekly pipeline audit's three near-duplicate groups before changing job records: Finastra `Customer Success Director` (Indeed 31265 / LinkedIn 31574, same Dubai role), Mace Group `Associate Director - Quality Assurance` (Indeed 32535/32684/32686, all Riyadh), and Publicis Groupe `Associate Creative Director` (32238 Riyadh / 32496 Dubai, likely a checker false positive because countries differ). Consider adding normalized location/country to the audit grouping.
