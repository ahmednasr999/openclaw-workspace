# 2026-05-29

- Raised `agents.defaults.compaction.reserveTokensFloor` in `/root/.openclaw/openclaw.json` from `40000` to `100000` after an auto-compaction recovery warning in the Telegram direct session. Backup: `/root/.openclaw/openclaw.json.pre-compaction-reserve-20260529-004236`. Config validation passed and gateway status remained healthy on OpenClaw `2026.5.27`.

## OpenClaw Gateway Hook Token and Compaction Closeout
- Set agents.defaults.compaction.reserveTokensFloor to 100000 after auto-compaction recovery warning.
- Added OPENCLAW_HOOKS_TOKEN via /root/.config/openclaw-hooks.env and systemd user drop-in /root/.config/systemd/user/openclaw-gateway.service.d/20-hooks-token.conf.
- Restarted the user gateway through a transient systemd timer. New gateway PID 2333127, OpenClaw 2026.5.27, config valid, gateway admin-capable.
- Update guard passed with report /root/.openclaw/workspace/reports/openclaw-update-guard-20260529-011511.txt. Gateway memory dropped from ~1.6-1.8 GiB to ~880 MiB after restart.
- Residual note: Ahmed-Mac node appeared on 2026.5.26 in gateway probe; VPS gateway is current.

## OpenClaw Ecosystem Adoption
- Ahmed approved moving ahead from deep ecosystem recommendations into execution.
- Created adoption register: `docs/architecture/openclaw-ecosystem-adoption-register-2026-05-29.md`.
- Verified live baseline: OpenClaw `2026.5.27 (27ae826)`, 118 skills, 79 model-visible, 78 command-available, 15 enabled plugins.
- Captured immediate backlog: runtime LLM support, skill allowlist matrix, telecrawl pilot, gitcrawl pilot, document-extract review, Tavily ecosystem radar template, and proof receipts.
- Created agent skill matrix: `docs/agent-governance/openclaw-agent-skill-matrix-2026-05-29.md`.
- Checked `telecrawl` and `gitcrawl` availability; neither binary was found in PATH or scanned OpenClaw/local binary paths, so pilots are blocked on install/source-path discovery.



## OpenClaw Crawler Pilot Execution
- Installed `gitcrawl` and `telecrawl` via Go into `/root/go/bin` and symlinked them into `/usr/local/bin`.
- Verified `gitcrawl` with an isolated HOME and synced one public `openclaw/openclaw` PR into SQLite. Result: 1 repository, 1 thread, database about 392K.
- Verified `telecrawl` CLI/status/doctor with an isolated DB, but the VPS has no Telegram Desktop source at `/root/.local/share/TelegramDesktop/tdata`, so import must move to Ahmed-Mac or a copied source.
- Created pilot report: `docs/research/openclaw-crawler-pilots-2026-05-29.md`.
- Added and verified reusable read-only digest script: `scripts/openclaw-gitcrawl-digest.py`; latest report: `reports/openclaw-gitcrawl-digest-2026-05-29.md`.

## OpenClaw Release Performance Gates
- Updated `scripts/openclaw-update-guard.py` after the lighter-core report to check install footprint, direct dependency count, nested duplicate OpenClaw dependency trees, native optional package count, optional cold/warm gateway turn latency, and plugin runtime LLM availability evidence.
- Verified with `python3 -m py_compile` and `scripts/openclaw-update-guard.py --json --write-report`. Latest report: `reports/openclaw-update-guard-20260529-030746.txt`.
- Result: release footprint passed at `505 MB`, `331` direct dependencies, `0` counted native optional package variants, and no nested duplicate OpenClaw dependency tree. Overall verdict remains WARN because `runtime.llm.complete` still lacks explicit availability evidence in this build.
- Optional latency probe passed: non-delivered gateway agent turns returned `pong`, cold `10.5s`, warm `9.6s`, both below guard thresholds.

## OpenClaw Cron Model Allowlist Fix
- CTO fixed stale cron model overrides after the runtime allowlist moved from `openai/gpt-5.5` to `openai-codex/gpt-5.5`.
- Backed up `/root/.openclaw/cron/jobs.json` to `/root/.openclaw/cron/jobs.json.bak-modelref-20260529-160450` and changed 20 stale cron payload model overrides.
- Validation reported: `openclaw config validate` passes, 0 stale `openai/gpt-5.5` cron payloads, 0 enabled cron payload models outside `agents.defaults.models`, and NASR Doctor (Daily) now shows `openai-codex/gpt-5.5`.
- Remaining risk: cron state still records the 07:00 failed runs until affected jobs rerun successfully.
