# 2026-04-26

## Prompt stack cleanup for GPT-5.5
- Updated `SOUL.md` and `AGENTS.md` to align with OpenAI GPT-5.5 prompt guidance: outcome-first instructions, retrieval budgets, concise preambles for tool-heavy work, and prompt-stack hygiene.
- Preserved hard invariants: approval gates, model-switch disclosure, confidentiality, no Mission Control, no em dashes, verification before done, core-file safety, and isolated sub-agent default.
- Backup created at `backups/core-prompt-cleanup-20260426-012103/`.
- Verification: backup-to-current diffs reviewed, invariant grep passed, edited files checked for em dashes.

## Propagated GPT-5.5 prompt hygiene to agent workspaces
- Backed up CMO, CTO, HR, and JobZoom prompt files to `backups/agent-prompt-cleanup-20260426-012810/`.
- CMO/CTO/HR `AGENTS.md` are symlinks to the main `AGENTS.md`, so the shared AGENTS baseline was already applied there.
- Added GPT-5.5 prompt hygiene blocks to `workspace-cmo/SOUL.md`, `workspace-cto/SOUL.md`, `workspace-hr/SOUL.md`, `workspace-jobzoom/SOUL.md`, and `workspace-jobzoom/AGENTS.md`.
- Replaced old "boil the ocean" style execution blocks with shorter outcome/success/verification standards while preserving role-specific guardrails.
- Verification: compared against backup, confirmed key invariants for each agent, confirmed no em dashes in edited files.
# 2026-04-26

## Prompt stack cleanup for GPT-5.5
- Updated `SOUL.md` and `AGENTS.md` to align with OpenAI GPT-5.5 prompt guidance: outcome-first instructions, retrieval budgets, concise preambles for tool-heavy work, and prompt-stack hygiene.
- Preserved hard invariants: approval gates, model-switch disclosure, confidentiality, no Mission Control, no em dashes, verification before done, core-file safety, and isolated sub-agent default.
- Backup created at `backups/core-prompt-cleanup-20260426-012103/`.
- Verification: backup-to-current diffs reviewed, invariant grep passed, edited files checked for em dashes.

## Propagated GPT-5.5 prompt hygiene to agent workspaces
- Backed up CMO, CTO, HR, and JobZoom prompt files to `backups/agent-prompt-cleanup-20260426-012810/`.
- CMO/CTO/HR `AGENTS.md` are symlinks to the main `AGENTS.md`, so the shared AGENTS baseline was already applied there.
- Added GPT-5.5 prompt hygiene blocks to `workspace-cmo/SOUL.md`, `workspace-cto/SOUL.md`, `workspace-hr/SOUL.md`, `workspace-jobzoom/SOUL.md`, and `workspace-jobzoom/AGENTS.md`.
- Replaced old "boil the ocean" style execution blocks with shorter outcome/success/verification standards while preserving role-specific guardrails.
- Verification: compared against backup, confirmed key invariants for each agent, confirmed no em dashes in edited files.

## X Intelligence Crawler fix and commit
- User asked whether X Intelligence Crawler work was done. Verified the final run completed successfully, Ahmed-Mac browser preflight passed, intel outputs were generated/updated, and no crawler process was running.
- User challenged why changes were not committed. Root cause: agent treated it as an operational fix and avoided committing because many runtime files were modified. Correction: clean scoped commit should have been done after verification.
- Commit `da7b4bc5` created with message `Fix X intelligence crawler browser flow`.
- Committed files: `scripts/x-intelligence-crawler.py`, `intel/intel-2026-04-26.md`.
- Left `intel/DAILY-INTEL.md` intentionally uncommitted as rolling daily pointer.
- Verification: `python3 -m py_compile scripts/x-intelligence-crawler.py` passed, repo preflight passed with 19 passed, 0 failed, 6 known missing-script warnings.

## Follow-through correction and scheduled checks
- User challenged why agent stopped instead of moving to the next remaining item. Lesson: after a successful scoped completion, interpret “done/go ahead” as continue to the next safe standing workstream unless blocked, and convert observational follow-ups into scheduled checks.
- Scheduled one-shot follow-ups:
  - JobZoom live run delivery observation for 05:15 Cairo (`2026-04-26T02:15:00Z`), report only meaningful result/blocker/failure.
  - Model Guardian timer observation for 06:20 Cairo (`2026-04-26T03:20:00Z`), confirm no false timeout alert.

## CMO content-gap consistency cleanup and commit
- Background CMO cleanup unified content-gap thresholds and docs.
- Policy now: urgent CEO alert threshold is any content gap in next 3 business days; planning/QA target is 5 business days of scheduled content; days 4-5 are backlog/QA warnings, not urgent pages.
- Removed conflicting “5 calendar days = emergency” wording.
- Commit `861d870b` created with message `Standardize CMO content gap thresholds`.
- Committed files: `scripts/cmo-desk-agent.py`, `skills/cmo-agent/HEARTBEAT.md`, `skills/cmo-agent/SKILL.md`, `skills/cmo-agent/eval/checklist.md`, `skills/cmo-agent/instructions/calendar.md`.
- Verification: `python3 -m py_compile scripts/cmo-desk-agent.py` passed, `git diff --check` passed, repo preflight passed with 19 passed, 0 failed, 6 known missing-script warnings.
- `.learnings/LEARNINGS.md` was updated for the follow-through lesson but not included in the CMO commit.

## Cron prompt migration workstream
- User approved continuing after CMO cleanup.
- Agent committed the learning note in `.learnings/LEARNINGS.md` as commit `beeb84d9` with message `Capture follow-through lesson`; warned the file had a larger pre-existing diff than expected.
- A subagent inventoried active large cron prompts. Active large prompts found: `X Intelligence Crawler - Weekly` length 1678, `CMO Weekly Drafting` length 1415.
- Disabled SAYYAD prompts were inspected and left read-only. Email Agent prompts >1000 chars were left untouched because they were outside priority and already script/formatter controlled.
- Key blocker noted: extracted files alone did not change cron payloads until gateway cron state was explicitly updated.
- User approved committing extracted cron skill files and migrating the two active cron payloads. Exact commit ID for extraction/migration is not in current compact context and should be recalled before asserting.

## JobZoom live run error investigation and fix
- User asked to dig deep into a non-fatal JobZoom error.
- Findings: the `scoring_quota_probe` health check timed out or failed with a non-429 error before recording a DB row, but actual batch scoring succeeded. It was not a quota issue.
- Evidence from today: 7 successful `batch_scoring` calls with HTTP 200; 13/13 jobs scored; 13/13 CVs generated; 0 provisional; 0 keyword fallback.
- Root cause: bad health-check observability and misleading wording. The probe was called quota, but only 429 truly means quota. Non-429 failures are gateway/model latency or request errors.
- User approved fix. Applied on disk in `/root/.openclaw/workspace-jobzoom/scripts/daily_run.py`:
  - Renamed phase from `scoring_quota_probe` to `scoring_health_check`.
  - Changed log wording from quota probe to pre-scoring model health check.
  - Added DB audit recording for request exceptions in `gpt_api_calls`, so timeouts are visible.
  - PDF report now marks AI Scoring as WARNING with a precise note only when health check fails but batch scoring succeeds.
- Verification: `python3 -m py_compile scripts/daily_run.py` passed; old quota-probe wording removed; simulated exception audit row inserted correctly and then removed.
- Maintenance note added: `workspace-jobzoom/reports/maintenance/jobzoom-health-check-fix-2026-04-26.md`.
- Caveat: `workspace-jobzoom` is ignored by the parent git repo, so changes are on disk but not commit-tracked unless force-added or workspace-jobzoom becomes its own repo.

## 2026-04-26 13:21 Cairo - Approval policy corrections and changes

- Ahmed corrected that read-only inspection commands should be pre-approved and should not interrupt him for manual approval.
- Ahmed also clarified that the Gmail job-search email agent should be pre-approved as a routine check, despite writing local summaries/pipeline state, because it does not send/delete/mark emails and is part of standing operations.
- I added explicit CEO allow patterns in `config/tool-permissions.yaml` for safe read-only inspections and routine jobs, including:
  - `ls`, `cat`, `grep`, `sed -n`, `head`, `tail`, `stat`, `df`, `du -sh`, `git status`, `git log`, `git diff --name-only`
  - `openclaw status`, `openclaw config validate`
  - `scripts/email-agent.py`, `scripts/format-email-alert.py`, `scripts/intel-sweep.py`, `scripts/nasr-doctor.py --fix`, session cleanup/watchdog scripts, LCM force compact, JobZoom daily run, CMO publishing watchdog, RSS intelligence crawler, weekly pipeline audit, weekly team retro, and CMO outreach queue weekly.
- Verification reported: `openclaw config validate` passed, diff showed only CEO allow-pattern changes.
- We deliberately did not broaden to `exec:*` and kept destructive/public/external-write actions gated: gateway restart/update/config apply, public LinkedIn posting unless exact scheduled path, third-party emails/messages, deletes/archives, job applications, credential changes, broad `rm/mv/chmod/chown`, `git push`, and unscheduled external writes.
- Related runtime checks completed: OpenClaw version `2026.4.24 (cbcfdf6)`, gateway ExecStart `/usr/bin/openclaw gateway --port 18789`. No restart was performed.

## 2026-04-26 14:47 Cairo - Known runtime patch: session-resume fallback prefix

- Ahmed approved adding the OpenClaw runtime patch to the known patches list alongside lossless-claw.
- Patch target: `/usr/lib/node_modules/openclaw/dist/bash-tools-MqL7r1OX.js`.
- Backup: `/usr/lib/node_modules/openclaw/dist/bash-tools-MqL7r1OX.js.bak.20260426143646`.
- Change: replaced `return "Automatic session resume failed, so sending the status directly.\\n\\n";` with `return "";`.
- Verification: grep found the string before patch, patch matched exactly once, post-patch verification reported `prefix_absent`, gateway was restarted, and background exec test completed without showing the ugly prefix.
- Caveat: this is a runtime `dist/` patch and OpenClaw updates can overwrite it. Recheck after updates and prefer a source-level fix with resume retry/backoff later.
- Upstream issue filed: https://github.com/openclaw/openclaw/issues/72143
