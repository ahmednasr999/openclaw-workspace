# Daily Notes - 2026-07-26

## OpenClaw staged hardening follow-up

- Diagnosed the weekly health warning. Gateway remained responsive with zero
  restarts and 16-51 ms health responses; six samples showed three isolated
  1.19-1.46 second maximum event-loop stalls but no sustained liveness warning
  or Telegram degradation.
- A parallel closeout run of several heavyweight OpenClaw CLI checks correlated
  with an 11.27 second maximum delay while the health response remained 33 ms,
  Telegram stayed connected, and gateway CPU was only 0.201 cores. This
  supports concurrent CLI/plugin initialization as an amplification trigger;
  avoid adding redundant high-frequency CLI probes.
- Classified the two standard security warnings as known trust-model trade-offs:
  full exec remains broad but protected by sandbox-all, workspace-only
  filesystem access, allowlisted channels, and Ahmed-only elevated access; the
  multi-user heuristic is caused by allowlisted group configuration.
- Reviewed the deep Composio finding: fixed-binary argument-array execution,
  trusted but high impact, not evidence of shell-string injection.
- Applied one no-restart hardening action: changed
  `/etc/systemd/system/camofox.service` from `0644` to `0600` because it contains
  an API credential.
- Deferred SSH/firewall/Camofox listener changes to an explicit access-safe
  maintenance window. Evidence shows public SSH with root password auth, no
  host firewall, and Camofox on all interfaces; Ahmed's Mac is online on
  Tailscale and recent successful logins used public-key authentication.
- Full report: `reports/security-hardening/2026-07-26.md`.
