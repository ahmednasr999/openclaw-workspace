# Daily Notes - 2026-05-01

# Daily Notes - 2026-05-01

## Pre-compaction durable memory flush - 14:41 Africa/Cairo

### Content-claw / premium LinkedIn visuals
- Added and committed content workflow docs for reference-led premium card production:
  - `88fb6670` - `docs: add image-to-ui premium card workflow`
  - `74a0d12b` - `docs: add AI agent workflow card template`
- Durable visual rule reinforced: use image/reference first for taste and direction, then controlled code/composition for typography, spacing, footer, and mobile fidelity. Do not mark visual work done from generation success or dimensions alone.
- Default LinkedIn visual reference remains `/root/.openclaw/workspace/output/jobzoom-visuals/ahmed-linkedin-ai-execution-card-4k.jpg`.
- AI-agent/automation posts should use `docs/content-claw/ai-agent-workflow-card-template.md` for content anatomy, then compress into Ahmed's 9:16 execution-card format. Do not copy dense horizontal dashboard layouts unless Ahmed asks.
- External generator target remains `/root/.openclaw/workspace-cmo/scripts/generate-premium-content-card.py`. It was patched externally to output `(2160, 3840)` and includes procedural theme/background logic, but it is ignored/untracked under git root `/root/.openclaw` because repo-wide `.gitignore` matches `*`.
- Generator state is not fully accepted: 9:16/dimensions pass, but premium craft parity with the reference is not proven. It remains a deterministic fallback/base layer, not an automatic final. Manual/local inspection is still required.
- Backup record committed: `53abc2e6` - `docs: record premium generator 9x16 patch`.

### X/social-post extraction and research artifacts
- X extraction remains fragile through normal browser/page fetches; `api.fxtwitter.com` was useful for reconstructing public tweet/article metadata and media.
- For X/LinkedIn posts with images, extract both post text and readable image text before summarizing or judging relevance.
- @LLMJunky X Article `2049871598883115376`, “Codex Guide: Images ⟶ Real UI in 6 Steps,” reinforced the visual workflow above.
- LudovicCreator post `2049855050613240060` produced a useful “AI Agent Workflow Card” anatomy: agent name, main job, inputs, workflow stages, tools, guardrails, human handoff, output, metrics/confidence.
- Adam Ludwin here.now tweet led to a safe evaluation, documented below.

### here.now safety evaluation
- Local installed here-now skill exists at `/root/.agents/skills/here-now/SKILL.md`, version observed as `1.6.3`.
- Safe anonymous publish test succeeded at `https://blissful-sketch-tqcr.here.now/`, with `auth_mode=anonymous`, `api_key_source=none`, and `persistence=expires_24h` until `2026-05-02T02:14:51.781Z`.
- Conclusion: here.now is acceptable for temporary public previews/throwaway shareable artifacts only. Do not use it for private OpenClaw memory, job-search data, CVs, credentials, or sensitive system context without stricter approval/security review.
- Committed doc: `b3fa3fdb` - `docs: record here.now safety evaluation`.

### JobZoom status and cron timeout
- Ahmed asked “How’s going with jobzoom?”, which was treated as explicit permission for read-only JobZoom inspection.
- 2026-05-01 JobZoom scan materially completed:
  - 150/150 searches successful
  - 354 eligible after exclusions
  - 275 after dedup
  - 64 after Pass 1
  - 4 live AI matches at 70+
  - 4 CVs generated
  - PDF report: `/root/.openclaw/workspace-jobzoom/reports/JobZoom_Daily_2026-05-01.pdf`
- Matched roles:
  1. Publicis Sapient - Agile Program Manager (Endur)-Senior Manager, UAE, score 72
  2. KinFitz & Co. - Project Manager (Microsoft), UAE, score 78
  3. Al Rostamani Group - IT Project Manager, UAE, score 76
  4. Müller's Solutions - ServiceNow SPM Project Manager, Saudi Arabia, score 78
- `report_delivered=0` was diagnosed as delivery/parent embedded-agent timeout after artifact generation, not scraping/scoring failure. No Telegram delivery was confirmed, no ZIP was created, and no media delivery artifacts were found.
- Batch scoring succeeded despite a separate non-fatal `scoring_health_check` timeout. Batch durations were slower than prior day but completed under batch budget.
- User approved adjusting the cron timeout. JobZoom Daily Scan cron is now intended/verified with `timeoutSeconds: 7200` (2 hours), schedule still `0 5 * * *` Africa/Cairo, delivery target `-1003882622947:5247`, model `openai-codex/gpt-5.5`. Scope, thresholds, prompts, and JobZoom code were not changed.
- Committed doc: `0f58370a` - `docs: record JobZoom cron timeout adjustment`.
- Follow-up needed after next 05:00 run: verify delivery flag, Telegram delivery, ZIP creation, and that the longer timeout prevents end-of-run interruption.

### Kernel/CVE watch and system health
- Kernel watch materially changed from “patched kernel not yet available” to resolved after later checks:
  - running kernel `6.17.0-23-generic`
  - relevant packages installed/candidate at `6.17.0-23.23`
  - no pending kernel upgrade for relevant packages
  - `/etc/modprobe.d/disable-algif.conf` exists with `install algif_aead /bin/false`
  - `algif_aead` module not loaded
  - port 631 has no listeners
- Conclusion: CVE kernel watch appears resolved, with mitigations still safe.
- OpenClaw health around later checks: gateway running/reachable, app `2026.4.29`, Telegram OK, patch checker green, active-memory direct FTS observed earlier (`directFts=1`).

### Daily profile drip / user preference
- Daily profile-drip pilot sent one Telegram DM question successfully to Ahmed (`messageId 52397`, chat `866838380`).
- Ahmed answered: `#7. Other : Salary`.
- Durable preference promoted: salary remains a primary decision factor and must not be excluded or treated as already resolved when comparing GCC executive roles.
- Files updated earlier in the day: `memory/daily-profile-drip.md` and `USER.md`.

### VPS disk cleanup and guard hardening
- Ahmed asked about VPS disk status. Diagnosis showed root disk at `81%` used (`78G / 96G`, `19G` free), inodes healthy, and disk pressure mainly from backups/snapshots rather than temp files.
- After Ahmed approved pruning, the following oversized items were deleted:
  - `/root/openclaw-backups/openclaw-pre-update-20260501-020425.tar.gz` (~12.2G)
  - `/root/.openclaw/backups/manual-update-20260428` (~6.5G)
  - `/root/openclaw-snapshot-20260430` (~5.1G)
- Post-prune verification: root disk improved to `57%` used (`55G / 96G`, `42G` free), about `23G` recovered; inodes still healthy at `11%`.
- Important correction: any older note saying `/root/.openclaw/backups/manual-update-20260428` was kept is now stale. It was deleted during the approved prune.
- Manifest path created: `docs/research/disk-prune-manifest-20260501-143024.md`.
- Follow-up health checks after cleanup:
  - cron enabled, store `/root/.openclaw/cron/jobs.json`, 37 jobs
  - OpenClaw status healthy, gateway running/reachable, app `2026.4.29`
  - runtime patch checker green
  - no gateway restart, runtime edit, or JobZoom change was made during disk hardening.
- `scripts/vps-disk-guard.sh` was hardened:
  - env thresholds: `DISK_GUARD_BACKUP_WARN_GB` default `8`, `DISK_GUARD_BACKUP_KEEP_DAYS` default `14`, `DISK_GUARD_PRUNE_BACKUPS` default `0`
  - audits candidates in `/root/openclaw-backups`, `/root/.openclaw/backups`, and `/root/openclaw-snapshot-*`
  - adds `--audit-backups` mode
  - destructive backup/snapshot pruning remains opt-in only and requires explicit approval via `DISK_GUARD_PRUNE_BACKUPS=1`
  - default threshold behavior now logs backup/snapshot audit and states pruning is disabled unless approved
- `memory/2026-04-30.md` was updated with a section documenting the disk-guard backup audit addition.
- Current disk-guard cron remains `VPS disk guard at 65%`, id `23316b4b-191b-4cd7-b290-aebc7ab4e1ca`, schedule `7 7 * * *` Africa/Cairo.
- Current uncommitted/unknown status after disk work: `scripts/vps-disk-guard.sh`, `memory/2026-04-30.md`, `memory/daily-profile-drip.md`, `USER.md`, and `docs/research/disk-prune-manifest-20260501-143024.md` were modified/created during the day; no commit was shown for the disk-guard/prune set in the visible transcript.

### Group chat docs takeaway
- OpenClaw group-chat docs were inspected. Durable takeaway for CEO/CTO/CMO/HR routing:
  - group chats are usually mention-gated by default
  - group replies are public to that group
  - group access/authorization is separate from DM pairing
  - keep separate session keys for group topics vs DMs to avoid context pollution
  - keep public/group behavior mention-gated or allowlisted unless Ahmed explicitly broadens it.
