# Daily Notes - 2026-04-24

# Daily Notes - 2026-04-24

## Pre-compaction flush - LinkedIn post, GPT-5.5, JobZoom, gateway

### LinkedIn post with premium visual
- Approved copy used:
  > The model is not the bottleneck. Execution systems are.
  >
  > The advantage is shifting from who has access to better AI, to who can turn it into reliable operating rhythm.
  >
  > Most leaders already have access to capable AI. What separates top performers is the infrastructure around it: clear inputs, structured prompts, automated feedback loops, and humans who know when to override the machine.
  >
  > The teams winning right now are not the ones with the most sophisticated agents. They are the ones who built the tightest loops.
  >
  > What is the execution system you wish you had three years ago?
  >
  > 💬 Drop it in the comments.
- Final chosen image: `/root/.openclaw/media/gpt55-execution-systems-premium-typographic.png`.
- Successful LinkedIn image post share URN: `urn:li:share:7453242377453993984`.
- User-facing link given: `https://www.linkedin.com/feed/update/7453242377453993984`.
- Earlier accidental text-only duplicate still exists: `urn:li:share:7453239326303363072`.
- Do not delete the duplicate without explicit user authorization.
- Technical lesson: `LINKEDIN_CREATE_LINKED_IN_POST` image `s3key` must be a Composio-managed storage key, not a LinkedIn image/asset URN. Successful storage key used: `project/pr_Gqp3256AsBat/tool_router_session/trs_5rS0BeaWMvWx/ra6tugKhBmju`.
- Transfer workaround used: Cloudflare quick tunnel to expose the local premium PNG so the sandbox could download and upload it into Composio storage.

### Model/router state
- Ahmed explicitly chose GPT-5.5 as the new default/primary for all jobs.
- `/root/.openclaw/workspace/config/model-router.json` was read and showed `default_model: openai-codex/gpt-5.5`, all rules/cost tiers pointing to GPT-5.5, and `auto_switch_back.enabled: false`.
- Model Guardian alert expecting GPT-5.4 is stale relative to Ahmed’s current preference and should be updated later if asked.

### X post extraction
- Steipete X post fetched via Composio browser workflow, session id `frog`.
- URL: `https://x.com/steipete/status/2047362803560841364`.
- Extracted content: Peter Steinberger (@steipete), tweet text “My favorite security advisories.” Media image text: “Steps To Reproduce: 1. Enable hooks.gmail.allowUnsafeExternalContent=true”. Timestamp 12:11 PM · Apr 23, 2026. Metrics at fetch: 56.3K views, 24 replies, 4 reposts, 433 likes, 53 bookmarks.

### JobZoom 0-live-jobs investigation
- 2026-04-24 JobZoom run did not find 0 raw jobs. It found/scored many intermediates: 341 filtered raw jobs, 311 deduped/new jobs, 91 Pass1/all-scored jobs, 0 live matches, 3 shadow jobs.
- Shadow file: `/root/.openclaw/workspace-jobzoom/reports/shadow/jobzoom-shadow-2026-04-24.json`.
- Shadow verdicts were keyword fallback around 60-63; live threshold is 70, shadow threshold is 60.
- Real JobZoom DB path: `/root/.openclaw/workspace-jobzoom/data/jobzoom.db`.
- Wrong accidental zero-byte DB path created earlier: `/root/.openclaw/workspace-jobzoom/jobzoom.db`.
- DB evidence showed 50 GPT API call records on 2026-04-24, all HTTP 500 Internal Server Error, 0 successes. The scoring layer exhausted the daily cap and fell back to keyword scoring; that is the immediate cause of 0 live matches.
- This was not because no jobs were scraped and not primarily because of the GPT-5.5 router switch. It was gateway `/v1/chat/completions` returning 500 for scoring calls.

### JobZoom patch applied
- Modified `/root/.openclaw/workspace-jobzoom/scripts/daily_run.py`.
- Changes:
  - Added `SCORING_MODEL = "openai-codex/gpt-5.5"`.
  - Raised `MAX_GPT_CALLS_PER_DAY` from 50 to 80.
  - `record_gpt_api_call()` now logs `SCORING_MODEL` instead of hardcoded `openai-codex/gpt-5.4`.
  - Reworded GPT-5.4-specific quota/log text to generic model wording.
  - Kept gateway request body using `"model": "openclaw/jobzoom"`, intentionally relying on gateway routing while logging expected underlying scoring model separately.
- Verification during patch: no remaining GPT-5.x wording in `daily_run.py`; final config showed `SCORING_MODEL = "openai-codex/gpt-5.5"` and `MAX_GPT_CALLS_PER_DAY = 80`.
- Daily GPT API counter in the JobZoom DB was reset from 50 to 0 to allow re-scoring once gateway is fixed.
- Re-scoring was blocked because the gateway HTTP chat completions path still returned 500.

### Gateway HTTP failure investigation
- Gateway health and `/v1/models` worked, but `/v1/chat/completions` returned plain `Internal Server Error` / HTTP 500 for all tested models/aliases, including `openclaw/jobzoom`, `openai-codex/gpt-5.5`, `gpt-5.5`, `gpt-5.4`, `openclaw/main`, and MiniMax.
- Therefore failure appears to be the gateway HTTP chat-completions route/core handling, not a single provider/model issue.
- `openclaw status` showed gateway reachable/service active, default GPT-5.5.
- Restart/SIGUSR1 attempts did not resolve the 500s.
- Process state discovered near compaction:
  - Old process `40775` still running from Apr 23 and listening on port `18789`.
  - New process `60871` existed after restart but was not listening on the port.
  - Only `40775` held port `18789`, so HTTP requests were still hitting the old/broken process.
- Next likely recovery action: safely kill stale listener `40775` or stop both gateway processes and start one fresh process, then verify `/v1/chat/completions` before re-running JobZoom scoring. Because gateway restarts are sensitive, follow gateway safety rules and avoid in-band destructive recovery without care.

### Behavior/process lessons
- Public/external actions need immediate acknowledgement and progress updates if taking more than a few minutes. The LinkedIn post debugging went too quiet, which frustrated Ahmed.
- If posting publicly with media, verify the actual resulting post has the expected media before claiming done.
