# 2026-07-27

## Content approvals

- Ahmed approved all four captions and visuals from the 2026-07-26 weekly LinkedIn batch.
- Live Notion already had scheduled posts on 2026-07-27, 2026-07-28, and 2026-07-30. The approved batch was scheduled without displacement:
  - 2026-07-29: Documentation Debt Becomes Operating Debt
  - 2026-08-02: More Reports Do Not Create Decisions
  - 2026-08-04: Payments Need a Recovery Design
  - 2026-08-06: PMO Scale Starts With Judgment
- All four rows passed publish-candidate preflight and resolved the exact visual through `asset.source=image_intent_final_asset`.
- Approval handling did not publish anything; the normal automated publishing lane remains responsible for duplicate checks, posting, and live URL verification.

## LinkedIn Comment Radar recovery

- Root cause of the 11:00 zero-candidate failure: the radar cached a stalled LinkedIn CDP renderer target even though Ahmed-Mac, the authenticated `nasr-linkedin` profile, and another LinkedIn tab were healthy.
- Repaired `/root/.openclaw/workspace-cmo/scripts/run_linkedin_comment_radar.py` to probe page targets, prefer a responsive feed/search target, quarantine stalled targets for the run, retry once, and create a fresh authenticated feed tab only when no existing page responds.
- Added a regression test for stalled-target failover.
- Live verification:
  - one direct permalink passed navigation, DOM extraction, and comment-control validation;
  - the full six-query recovery run returned code 0 with status `ready_full_pack`;
  - 18 candidates extracted, 18/18 URL coverage, 5/5 live validations passed, and 5 approval cards produced;
  - no LinkedIn post, like, comment, message, or other external action was performed.

## LinkedIn Radar production workflow contract

- Ahmed approved implementing the loop-to-graph recommendation with LinkedIn Comment Radar as the reference workflow.
- Added the reusable persisted-stage contract at `scripts/production_workflow_contract.py` and the governance decision at `docs/agent-governance/production-workflow-contract-2026-07-27.md`.
- Converted the radar to five immutable stages: `source -> extract -> validate -> rank -> approval`.
- Each run now records canonical inputs, stage attempts, timestamps, classified failures, immutable JSON outputs and SHA-256 hashes, artifact hashes, judge evidence, and an explicit terminal state.
- Added automatic same-date/slot resume, explicit `--resume`, `--no-auto-resume`, two-attempt exhaustion, artifact overwrite protection, safe run/stage identifier checks, and a 30-second authenticated-browser lock timeout.
- Added the standalone read-only judge at `/root/.openclaw/workspace-cmo/scripts/judge_linkedin_comment_radar_run.py`. It requires five unique approval cards, usable LinkedIn URLs, live permalink/comment-control evidence, verified draft sources and context hashes, consistent commands, and matching report/card/pack and stage hashes.
- Verification:
  - 7 reusable contract tests and 26 radar/judge tests passed.
  - Deliberately broken fixtures for missing URL, duplicate candidate, context mismatch, corrupt stage evidence, and missing approval pack were rejected.
  - A crash injected after `source` proved resume reused the completed source output and retried only `extract`.
  - Live lock-contention sample `2026-07-27-1500-recovery-2` stopped after 30 seconds as resumable `blocked` because the approved LinkedIn +30 campaign owned the browser lock; `source` recorded `TimeoutError`, all later stages stayed pending, and no report/card/pack was created.
- No LinkedIn action, cron change, gateway/runtime change, or campaign interruption occurred.
- Residual proof: run one read-only resume/full-pack sample after the LinkedIn +30 campaign releases the shared browser lock before migrating the contract to another workflow.
