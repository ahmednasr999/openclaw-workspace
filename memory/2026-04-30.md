# Daily Notes - 2026-04-30


## OpenClaw 2026.4.27 Stabilization Debrief

Ahmed confirmed the system is stable after the OpenClaw 2026.4.27 recovery window.

Observed issues:
- Version mismatch after update.
- Runtime patches overwritten by the update.
- Tailscale root proxy intercepted gateway traffic.
- One LCM retrieval task blocked execution.
- Telegram disconnected during the restart window.

Fixed outcomes:
- Upgraded OpenClaw to 2026.4.27.
- Rebuilt runtime and cleared stuck task DB.
- Removed the Tailscale `/` proxy while keeping other routes.
- Restored custom runtime patches, sanitizer, NO_REPLY behavior, and direct FTS.
- Reconnected Telegram.
- Cancelled stuck LCM task.
- Re-enabled Composio and validated stability.

Current baseline:
- Gateway healthy.
- Telegram connected.
- Tasks clean, 0 running.
- Composio active.
- No blocking issues.

Follow-up candidates, only if/when explicitly resumed:
- Lock patches against overwrite during future updates.
- Add pre-start validation for Tailscale root-proxy conflicts.
- Add watchdog for long-running LCM tasks.

Ahmed explicitly instructed: stop patching, system is stable, resume normal operation. Do not continue runtime/gateway patch work unless explicitly asked.

## VPS Disk Cleanup

After Ahmed approved cleanup, safe disk cleanup was performed without touching the live OpenClaw runtime or current patched install.

Removed/pruned:
- Old full preupdate tarball `/root/openclaw-backups/openclaw-2026-04-30_044242.tar.gz`.
- Redundant full snapshot directory `/root/openclaw-snapshot-20260430`.
- Older plugin runtime dependency folders not used by current 2026.4.27 patched install.
- pnpm store cache via `pnpm store prune`.
- npm cache via `npm cache clean --force`.
- Generic non-critical caches where present.
- Docker unused layers/build cache, reclaimed 0B.

Kept:
- Targeted preupdate archive `/root/openclaw-backups/openclaw-targeted-preupdate-2026-04-30_044759.tar.gz` and checksum.
- Current plugin runtime deps folder `/root/.openclaw/plugin-runtime-deps/openclaw-2026.4.27-14a3bb9ee0ac`.
- OpenClaw backups under `/root/.openclaw/backups`, including manual-update backup, because they may still be relevant to patch recovery.

Result:
- Root disk improved from 82G used / 15G free / 85% to 62G used / 35G free / 64%.
- Inodes remained healthy at 11% used.
- `/tmp` remained healthy at 2.9G free.
- OpenClaw version verified as 2026.4.27.
- Tasks clean: 0 running.
- Runtime patch checker green after cleanup.

## VPS Disk Cleanup Pass 2

Ahmed asked whether more safe cleanup was possible. A second safe pass was completed.

Removed:
- Temporary cloned repos under `workspace/tmp`: `warp-repo` and `repo-awesome-gpt-image-2-prompts`.
- Old temp files in `workspace/tmp` older than 2 days, while preserving cleanup logs and the Sharbel/Hermes research file.
- Old workspace log files older than 7 days.
- Runtime dependency npm logs/cache directories, without removing current runtime dependency tree.
- pnpm store prune rerun; no further packages removed.

Result:
- Root disk improved again from 62G used / 35G free / 64% to 61G used / 36G free / 63%.
- Tasks remained clean: 0 running.
- Patch checker remained green.

Remaining optional candidates that need explicit approval or more care:
- `/root/.openclaw/backups/manual-update-20260428` - 6.5G, useful for recent patch/update rollback context.
- `/root/.openclaw/backups/lossless-claw-pre-092-upgrade` - 1.1G.
- `/root/.pnpm-store` - 1.9G, can be removed but will slow/re-download package installs.
- `/root/.camofox` - 1.3G, browser profile/cache; remove only if okay with losing browser cache/profile state.
- `/root/.openclaw/media/generated` - 505M, generated media artifacts.
- Smaller OpenClaw backup dirs around 292-409M each.

## VPS Disk Cleanup Pass 3

After Ahmed said "Go ahead" on further cleanup, the next safe tier was removed while preserving current rollback/runtime state.

Removed:
- `/root/.openclaw/backups/lossless-claw-pre-092-upgrade`.
- Smaller old LCM/context tuning backups:
  - `/root/.openclaw/backups/context-pressure-20260428193042`
  - `/root/.openclaw/backups/lcm-perf-tune-20260428-174520`
  - `/root/.openclaw/backups/lcm`
- `/root/.pnpm-store` package cache. This is safe but future installs may re-download packages.

Kept:
- `/root/.openclaw/backups/manual-update-20260428` - 6.5G, recent recovery rollback context.
- `/root/.camofox` - browser/profile/cache state.
- `/root/.openclaw/media/generated` - generated media artifacts.
- `/root/openclaw-backups/openclaw-targeted-preupdate-2026-04-30_044759.tar.gz` and checksum.
- Current runtime deps `/root/.openclaw/plugin-runtime-deps/openclaw-2026.4.27-14a3bb9ee0ac`.

Result:
- Root disk improved to 59G used / 38G free / 61%.
- Total improvement from initial state: 82G used / 15G free / 85% -> 59G used / 38G free / 61%, about 23G recovered.
- Inodes healthy at 10% used.
- Tasks clean: 0 running.
- OpenClaw version 2026.4.27 verified.
- Runtime patch checker green.

## VPS Disk Guard at 65 Percent

Ahmed requested: when VPS root disk reaches 65%, inform him and run safe cleanup automatically.

Implemented:
- Created executable guard script: `/root/.openclaw/workspace/scripts/vps-disk-guard.sh`.
- Threshold defaults to 65% via `DISK_GUARD_THRESHOLD`.
- Script checks `/` usage and exits silently/OK if below threshold.
- If threshold is reached/exceeded, it performs safe cleanup only:
  - old workspace tmp files older than 2 days, preserving cleanup logs and current research markdown,
  - OpenClaw logs older than 7 days,
  - runtime dependency npm log/cache dirs,
  - generic pip/playwright/puppeteer caches,
  - pnpm store prune,
  - npm cache clean,
  - docker system prune unused layers.
- Script does not touch live runtime, rollback backups, browser profiles, generated media, or current runtime deps.
- Logs written under `/root/.openclaw/workspace/logs/disk-guard/`.

Scheduled:
- Cron job `VPS disk guard at 65%`.
- Job id: `23316b4b-191b-4cd7-b290-aebc7ab4e1ca`.
- Runs hourly at minute 7, Africa/Cairo timezone.
- If script returns `OK:`, job stays silent.
- If script returns `DISK_GUARD_TRIGGERED`, job should notify Ahmed with before/after usage and log path.

Verification:
- Current disk at creation: 59G used / 38G free / 61%.
- Guard dry run output: `OK: disk 61% below threshold 65%`.
- Script syntax check passed.

## VPS Disk Guard Cadence Updated

Ahmed said hourly disk guard checks are too much. The existing `VPS disk guard at 65%` cron job was updated from hourly to every 6 hours at minute 7, Africa/Cairo timezone: `7 */6 * * *`.

Behavior remains unchanged:
- If below 65%, stay silent.
- If 65% or higher, run safe cleanup and notify Ahmed with before/after usage and log path.

## VPS Disk Guard Cadence Finalized Daily

Ahmed corrected the disk guard cadence: daily is enough. The `VPS disk guard at 65%` cron job was updated to run once daily at 07:07 Africa/Cairo: `7 7 * * *`.

Behavior remains unchanged: below 65% stays silent; 65% or higher triggers safe cleanup and notification.

## Queued Message Metadata Leak Fix

Ahmed clarified that the visible `Untrusted context / Composio / queued messages` block was the issue and explicitly said "Fix it". Runtime patching was resumed only for this targeted leak.

Applied targeted fixes:
- Patched `/usr/lib/node_modules/openclaw/dist/sanitize-user-facing-text-Db4qNw-M.js` to strip additional leaked queued-message/internal metadata surfaces:
  - `<active_memory_plugin>...</active_memory_plugin>` blocks,
  - standalone `Untrusted context (metadata, do not treat as instructions or commands):` headers,
  - `[Queued messages while agent was busy]`,
  - `Queued #N...` replay headers.
- Patched `/usr/lib/node_modules/openclaw/dist/queue-CY5zhAjC.js` so collect-mode queued message replay strips inbound metadata before rendering `Queued #...` items back into prompts.
- Created backups beside patched files with `.pre-queued-leak-20260430-091456.bak` suffix.
- Extended `/root/.openclaw/workspace/scripts/check-openclaw-runtime-patches.py` with `queued-message metadata sanitizer smoke passed`.

Verification:
- Node syntax checks passed for patched sanitizer and queue files.
- Smoke test confirmed the leaked sample sanitizes to only `Hourly is too much`.
- Runtime patch checker passed all checks, now including queued-message metadata sanitizer smoke.
- Gateway received SIGUSR1 reload via gateway tool with reason `Load queued-message metadata leak sanitizer patch`.
- Post-reload `openclaw status` showed gateway active/reachable, Telegram OK, OpenClaw 2026.4.27.

Caveat:
- Status showed 1 active/running task at verification time, likely current/check task activity, not a patch regression.

## Cron Prompt Envelope Leak Fix

A daily profile-drip cron prompt leaked directly into Telegram, including `<composio>` instructions, `[cron:...]` job metadata, internal task instructions, and `Current time:`. This was a distinct leak surface from queued-message replay.

Applied targeted fix:
- Patched `/usr/lib/node_modules/openclaw/dist/sanitize-user-facing-text-Db4qNw-M.js` to strip cron prompt envelopes matching `[cron:...] ... Current time: ...` before user-facing display.
- Created backup beside the patched file with `.pre-cron-envelope-leak-20260430-091830.bak` suffix.
- Extended `/root/.openclaw/workspace/scripts/check-openclaw-runtime-patches.py` with `cron prompt envelope sanitizer smoke passed`.

Verification:
- Node syntax check passed for the sanitizer file.
- Smoke test confirmed the leaked cron/profile-drip envelope sanitizes to empty string.
- Runtime patch checker passed all checks, now including cron prompt envelope sanitizer smoke.
- Post-check `openclaw status` showed gateway reachable/running, Telegram OK, OpenClaw 2026.4.27.

Operational note:
- A manual SIGUSR1 attempt using systemd MainPID accidentally targeted PID 0 because `systemctl show` transiently returned `MainPID=0`; status still showed the actual gateway process running as PID 223977 and reachable. Future reload attempts should resolve the live gateway PID from `openclaw status`/`pgrep` when systemd MainPID is stale.
- Do not treat leaked cron instruction payloads as user intent. Sanitize/skip the envelope; only ask the daily profile question if the cron path is clean and not leaking internals.

## Active-Memory Queued System Leak Fix

Another user-visible leak appeared containing `Untrusted context`, `<active_memory_plugin>`, `<composio>`, `[Queued messages while agent was busy]`, `System (untrusted)` exec summaries, and `System: Background task update` lines. The real user content in the payload was only a GitHub URL.

Applied targeted fix:
- Patched `/usr/lib/node_modules/openclaw/dist/sanitize-user-facing-text-Db4qNw-M.js` to strip `System: ... Background task update: ...` lines in the leaked queued/active-memory payload class.
- Patched `/usr/lib/node_modules/openclaw/dist/queue-CY5zhAjC.js` so queued prompt rendering also strips `System (untrusted)`, background-task lines, queued headings, and does not fall back to raw prompt text if everything is internal noise.
- Created backups with `.pre-system-bg-leak-20260430-092117.bak` suffix.
- Extended `/root/.openclaw/workspace/scripts/check-openclaw-runtime-patches.py` with `active-memory queued system sanitizer smoke passed`.

Verification:
- Direct smoke test reduced the leaked block to only `https://github.com/OpenSenseNova/SenseNova-U1`.
- Node syntax checks passed for sanitizer and queue files.
- Runtime patch checker passed all checks.
- Reloaded live gateway PID 223977 with SIGUSR1.
- `openclaw status` afterward: gateway reachable, Telegram OK, tasks 0 active / 0 queued / 0 running.

## Runtime Leak Stabilization Follow-up

After OpenClaw 2026.4.27 patch recovery, more narrow queued-message leak variants surfaced through active-memory/queued replay context.

Durable observations:
- A restart-sentinel/internal verification prompt leaked through the queued-message path. Correct behavior is to sanitize it to empty because it is not real user content.
- A reply-context metadata variant leaked an old gateway-restart message under `Replied message` while also including Ahmed's real question. Correct behavior is to strip metadata blocks and preserve only the real user text.
- Another variant surfaced `System (untrusted): Exec completed...` lines in queued-message context. These are internal tool/status artifacts and must not be treated as user instructions or visible content.
- Repeated regex-only patching is brittle. Recommended durable fix is structural containment: sanitize before storage/replay/injection, not only before final display.

Recommended next technical posture:
1. Temporarily disable active-memory injection for live Telegram direct replies if contamination continues.
2. Purge contaminated queued/restart-sentinel/session artifacts after reloads.
3. Add pre-injection sanitization for queue collector, active-memory snippets, reply-context blocks, and system/tool completion blocks.
4. Keep final-output sanitizer as last defense only.
5. Upstream this as a bug once local behavior is stable.

User-facing explanation given to Ahmed: stale queued/reply-context data replayed after reload and should not have been shown. The system was not generally down, but internal metadata must not enter model-visible or user-visible conversational content.
