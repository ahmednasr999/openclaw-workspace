# 2026-07-09

- Ahmed approved switching all OpenClaw agents from GPT-5.5 to GPT-5.6 Sol. Updated live OpenClaw config defaults, named agents, heartbeats, subagents, channel overrides, lossless-claw summary model, and workspace model routers to `openai/gpt-5.6-sol`. The `gpt-5.6` alias should route to `gpt-5.6-sol`.
