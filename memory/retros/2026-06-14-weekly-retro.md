# Weekly Team Retro

**Period:** 2026-06-07 to 2026-06-14
**Generated:** 2026-06-14 10:26 Cairo

## Summary

| Metric | Value |
|--------|-------|
| Total Runs | 64 |
| Successes | 64 |
| Failures | 0 |
| Overall Success Rate | 100.0% |
| Active Agents | 6 |

## Agent Performance

| Agent | Runs | Success Rate | Avg Duration | Avg Records |
|-------|------|--------------|--------------|-------------|
| email-agent | 30 | ✅ 100.0% | 33.4s | 16.7 |
| jobs-source-linkedin-jobspy | 12 | ✅ 100.0% | 1790.8s | 514.5 |
| jobs-merge | 6 | ✅ 100.0% | 114.6s | 23.2 |
| jobs-review | 6 | ✅ 100.0% | 151.0s | 10.0 |
| jobs-source-indeed | 5 | ✅ 100.0% | 43.3s | 12.2 |
| jobs-source-google | 5 | ✅ 100.0% | 63.8s | 1.0 |

## Top Performers

**Most Reliable:** email-agent (100.0% success rate across 30 runs)
**Fastest:** email-agent (avg 33.4s)
**Most Productive:** jobs-source-linkedin-jobspy (avg 514.5 records/run)

## Lessons Learned This Period

**2026-06-12:**
- Incident: OpenClaw 2026.6.6 update preflight failed before any install or restart because `scripts/openclaw-update-guard.py --write-report` found `openai/gpt-5.5` in `/root/.openclaw/openclaw.json` and `/root/.openclaw/workspace/config/model-router.json` while expecting `openai-codex/gpt-5.5`; codex usage also did not show `openai-codex`.
- Do differently: Before updating or restarting OpenClaw, run the update guard and inspect its report. If model refs fail, repair provider namespace drift in both the live config and workspace router, validate config, rerun the guard, and do not install or restart while the verdict is FAIL.

**2026-06-12:**
- Correction: Ahmed had to resend an email-agent cron task because the previous response only acknowledged it instead of completing the original scheduled work.
- Do differently: For cron/reminder tasks, run the requested tools or scripts immediately and return only the requested final output or summary. Do not send `on it` or acknowledgement/status text as the answer, and wait for spawned or background work before closing.

**2026-06-13:**
- Incident: A gateway verification command ran `diff -u ... | sed ...` under `set -euo pipefail` after successfully patching `scripts/vps-disk-guard.sh`. Because `diff` exits 1 when files differ, OpenClaw marked the command as a tool error even though the diff output was the expected verification artifact.
- Do differently: When a command intentionally shows a diff, wrap the diff with `|| true` or temporarily disable `set -e` around it, and keep syntax/tests as the real pass/fail gates.

**2026-06-13:**
- Incident: Broad `rg`/log reads across session JSONL files returned huge embedded tool schemas and payloads, causing `Tool output unavailable due to post-processing error` instead of useful evidence.
- Do differently: For lessons capture or session review, search only today's non-trajectory session files and use a JSON-aware extractor with bounded snippets instead of dumping broad raw matches from all sessions.

**2026-06-13:**
- Preference: Ahmed said the current OpenClaw version is stable and approved deleting `/root/.openclaw/workspace/backups/openclaw-update-20260612-214914`; deletion freed about 2.4G and was followed by successful `openclaw config validate` and gateway probe checks.
- Do differently: Do not assume that June 12 rollback backup exists for future recovery. Create or verify a newer backup/snapshot before any risky OpenClaw update or rollback-dependent change.

## Growth Areas

**Performance optimization candidates:**
- jobs-source-linkedin-jobspy: avg 1790.8s per run
- jobs-merge: avg 114.6s per run
- jobs-review: avg 151.0s per run
- jobs-source-google: avg 63.8s per run

## Recommendations

- Consider parallelizing or caching for jobs-source-linkedin-jobspy (slowest agent)

---
*Generated by weekly-team-retro.py*