# 2026-07-15

- Corrected a live email-alert false positive after CNN newsletter UID `364999` was surfaced as pipeline `TP`. Root cause was unrestricted normalized substring matching: `TP` matched across the `It - Pilot` word boundary, while `newsletters.cnn.com` was absent from the sender-noise patterns.
- Repaired the classifier with token-boundary matching, corroboration requirements for aliases shorter than four characters, and plural newsletter-subdomain noise detection. Added exact CNN, `TP-Link`, short-alias-only, corroborated short-alias, full-company, and no-delivery-envelope regressions.
- Verification passed: Python compilation, 66/66 core email tests, synthetic harness, delivery-ledger worker test, exact CNN replay with zero pipeline match/score/envelopes, and live watcher health with zero pending alerts. No service or gateway restart was required because each notification launches a fresh classifier subprocess.
- No email was sent or replied to.
- Reapplied the OpenClaw 2026.7.1 runtime-hardening patch set after the update had restored unpatched bundles. Backed up the 11 affected runtime files plus the systemd runtime-hardening drop-in under `/root/.openclaw/backups/runtime-patches-20260715-102732`.
- Restart verification passed on the live `/usr/lib/node_modules/openclaw/dist` runtime: all runtime patch checks green, gateway probe healthy and admin-capable, Telegram connected, configuration valid, health dashboard `OK`, and no warning-level gateway journal entries after startup.
- Installed the upstream `openclaw/agent-skills` autoreview skill from commit `599be8dcd33369ce06324cdb63da2e421830fac2` and made it the execution engine behind `codex-review-closeout`.
- Applied the approved NASR policy patch: Codex defaults to `gpt-5.6-sol`, automatic Sol-to-Terra fallback is disabled at configuration and execution layers, and any programmatic Codex fallback is rejected visibly. Claude fallback remains explicit and opt-in.
- Verification passed: Python compilation, autoreview deterministic self-test, 27 unit tests, and 219 hardening tests. After Ahmed approved OAuth activation, the standalone Codex CLI was authenticated through the official device flow using his existing Google/ChatGPT session. A live benign harness ran on `gpt-5.6-sol` with high reasoning and returned clean with no actionable findings; no fallback occurred.
- Added the Memory Heist containment guard for built-in `web_fetch`. The network boundary now fails closed and accepts only an exact HTTP(S) URL from the current user prompt or an exact structured URL returned by `web_search` in the same run. Path/query mutations, character-by-character paths, encoded-memory URLs, deceptive snippet URLs, missing guard wiring, and user-agent variation are rejected.
- Hardened URL provenance parsing through eight reviewed iterations. The final v8 policy deliberately rejects ambiguous URL tokens ending in Markdown-sensitive markers (`*`, `_`, `~`) instead of attempting CommonMark parsing; literal terminal markers must be percent-encoded. Apostrophes inside paths, balanced parentheses, escaped closers, prose punctuation, typographic quotes, and Markdown link destinations have regression coverage.
- Updated `scripts/reapply-openclaw-2026-5-18-runtime-patches.py`, `scripts/check-openclaw-runtime-patches.py`, and `docs/runtime-patches/web-fetch-url-provenance.md`. Verified Python compilation, clean diff whitespace, cloned-runtime upgrade, live on-disk upgrade, checker coverage, reapply idempotence (`patched_files=0`), valid OpenClaw config, and final focused autoreview clean at 96% confidence.
- Backups were created for each live-bundle revision; final pre-v8 backup is `/root/.openclaw/backups/webfetch-provenance-v8-20260715/openclaw-tools-CIBcX9Ku.js.v7`. No gateway restart was performed. The current gateway process started at 18:15, before the final bundle mtime at 19:06, so v8 is installed on disk and will load on the next approved restart; the earlier provenance guard remains loaded meanwhile.
- Deferred unrelated review finding: the existing Claude CLI `runClaudeCliAuthPreflight` runtime patch is only a marker/guidance function, not a true auth gate. Fixing that requires a separate Claude bridge task and was not mixed into the web-fetch security change.
- Ahmed approved the controlled GPT-Red pilot. Built an isolated attacker/evaluator
  under `labs/gpt-red-pilot` against the Memory Heist egress-provenance policy. The
  baseline reproducibly promoted an attacker URL nested inside an object-valued
  search snippet. The isolated candidate narrowed promotion to direct search-result
  URL fields and added JSON-size, depth, and node budgets. Candidate training passed
  7/7 gated scenarios, held-out passed 6/6, and the combined original/pilot Node suite
  passed 16/16. No enabled plugin, gateway, configuration, core-memory, or live-runtime
  change was made; production promotion remains a separate reviewed repair step.
- Reviewed and ingested OpenAI's "GPT-Red: Unlocking Self-Improvement for Robustness" into the isolated Obsidian AIOS pilot vault. Captured the first-party source, reviewed claims, a proposed controlled red-team improvement loop, risks, an index view, and a practical assessment. The key conclusion is that GPT-Red demonstrates a human-controlled attacker-to-training-data safety flywheel, not autonomous recursive self-improvement. No core-memory, gateway, model, or live-runtime change was made; vault validation passed with 10 Markdown files and no broken links.
