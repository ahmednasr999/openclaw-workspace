# 2026-06-25

## CMO Loop Engineering post review handoff

- Created review-ready CMO draft for `AI prompting was phase one. Loop Engineering is phase two.`
- Local draft: `/root/.openclaw/workspace-cmo/drafts/2026-06-25-loop-engineering-phase-two.md`.
- Visual: `/root/.openclaw/workspace-cmo/drafts/images/loop-engineering-creative-reference-concept-final-1280x659.png`, copied from Ahmed-approved final visual.
- Notion Content Calendar page: `3898d599-a162-81d2-b956-ca7023c4a688`, status `Review`, owner `Ahmed`, post URL empty.
- Publishing remains approval-required.

## Telegram rich messages enabled

- Ahmed approved enabling Telegram rich messages for OpenClaw.
- Set `channels.telegram.richMessages: true` in `/root/.openclaw/openclaw.json`.
- Backup before change: `/root/.openclaw/openclaw.json.bak.telegram-rich-20260625-091917`.
- Verified `openclaw config validate`, gateway health OK, Telegram connected, and `/status` equivalent reports `Telegram rich messages: on` with Bot API 10.1 `sendRichMessage` enabled.
- Existing non-blocking warnings remain: legacy plugin install metadata conflict for `codex`, `lossless-claw`, `tokenjuice`, plus the gateway service PATH recommendation.

## Telegram rich messages default preference

- Ahmed said to always use Telegram rich messages.
- Use rich formatting by default for Telegram replies when it improves readability, especially reports, diagnostics, JobZoom summaries, and structured status cards.
- Fall back to plain text only if Telegram shows unsupported rich-message output or delivery reliability degrades.

## VPS disk safe cleanup

- Ahmed asked for maximum and safest cleanup after disk reached about `78G/96G` used, `81%`.
- Cleaned safe caches only: apt package/cache files, `/tmp` old items, OpenClaw/node compile caches, Hermes npm/cache/playwright cache, and old Chrome backup cache folders.
- Docker prune reported `0B` reclaimed despite `docker system df` showing image layers as reclaimable, because running containers still reference those layers.
- SQLite `lcm.db` and Codex logs were inspected for free pages; VACUUM was skipped because free-page reclaim was negligible and live runtime risk was not justified.
- Final verified disk state: about `76G/96G` used, `20G` free, `80%`; gateway health OK and config valid.
- Remaining large safe-sensitive areas: OpenClaw agent state/logs `13G`, `/root/.openclaw/lcm.db` about `4.1G`, main Codex logs about `3.9G`, and Hermes pilot workspace about `6.4G`.
## VPS disk cleanup follow-up

- Ahmed challenged the disk trend because free space was around 40G a couple of weeks earlier.
- Found two root-level OpenClaw snapshots totaling about 9.1G: `/root/openclaw-snapshot-20260623.tar.zst` and `/root/openclaw-snapshot-20260624.tar.zst`.
- Removed only the older snapshot and kept the latest backup, matching the standing rule to keep only the latest backup.
- Final verified root disk: `96G` total, `72G` used, `25G` free, `75%` usage.
- Gateway health and config validation remained OK.

## VPS disk retention policy applied

- Ahmed approved implementing the recommended VPS disk retention plan.
- Tightened OpenClaw snapshot retention to keep only the latest snapshot archive.
- Added archive-before-delete cleanup for nested Codex rollout transcripts under `agents/*/agent/codex-home/sessions`.
- First run archived 5,225 old Codex transcript files, preserving about 2.4G raw transcript data as compressed archives under `/root/.openclaw/session-archives`.
- Removed old duplicate Hermes workspace virtualenvs while preserving the active `/srv/hermes-pilot/venv`, workspace `.venv`, and `.venv-run`.
- Added weekly VPS disk report cron for Sundays 09:35 Cairo to Ahmed DM.
- Verification: root disk now about 70G used / 27G free / 73%, inodes 10%, OpenClaw config valid, gateway health OK.
- Residual issue: Codex SQLite log compaction did not reclaim space because row counts/file sizes stayed unchanged; deeper log retention needs a separate schema-aware fix if required.

## Codex log DB retention cleanup

- Ahmed approved continuing the VPS disk cleanup into Codex SQLite log retention.
- Took verified SQLite backups for all Codex log DBs under `/root/.openclaw/backups/codex-logdb-20260625-110822`, compressed size about `396M`.
- Applied schema-aware log retention in `scripts/vps-disk-guard.sh`: preserve `WARN`/`ERROR`, preserve recent logs, prune high-volume trace/debug/noisy telemetry, checkpoint, and vacuum.
- Verified `PRAGMA quick_check` OK for main, cmo, cto, hr, and jobzoom log DBs after compaction.
- Final verified disk: `96G` total, `67G` used, `30G` free, `70%` usage; inodes `10%` used.
- Final Codex log DB sizes: main about `1.6G`, cmo `160M`, cto `117M`, hr `136M`, jobzoom `117M`.
- OpenClaw config valid, gateway health OK, disk guard and session cleanup validations OK.
## OpenClaw Health Guard LCM compaction

- Health Guard reported CRITICAL because active LCM context for Telegram DM conversation `9713` was above the `120k` threshold.
- OpenClaw session compaction reduced stored session tokens from about `177k` to `122k`, but LCM health still remained slightly above threshold.
- Ran the LCM compact processor with a one-item queue for conversation `9713`, using the LCM engine rather than direct row deletion.
- Result: LCM context dropped from `122,341` tokens to `14,139`; context items dropped to `25` with `16` raw messages and `9` summaries.
- Fresh health dashboard report: overall `OK`; gateway, config, runtime patches, recent logs, dashboard service, and LCM context all OK.

