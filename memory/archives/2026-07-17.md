# 2026-07-17

## HR Career Sentinel v1 production activation

- Ahmed explicitly approved enabling HR Career Sentinel.
- Activation gates passed: 17/17 Sentinel tests, Gmail Pub/Sub ledger regression, systemd validation, and Memory Heist security suite 19/19 before and after verification.
- Pre-state inspection found `hr-career-sentinel.service` already installed, enabled, and running since 00:24:48 Cairo. The installed unit checksum matched the reviewed repository unit, so NASR did not overwrite or restart it.
- Live evidence: zero restarts, SQLite integrity `ok`, 32 Pub/Sub notifications completed, two Gmail messages/threads recorded, zero processing/delivery errors, and no alerts emitted for the initial non-actionable intake.
- Peak memory was about 114.2 MiB under the 384 MiB service limit.
- Legacy Email Agent schedules, Email Health Card, Email Hygiene, Email Synthetic Regression, OpenClaw Health Guard, `gog-gmail-watch.service`, and `gog-calendar-watch.service` remain disabled/inactive. Gmail watch renewal remains enabled.
- Sentinel uses `openai/gpt-5.6-sol` with medium/high reasoning by policy. No gateway, model-router, cron, Telegram, or Gmail configuration was changed.
- A direct post-activation GPT-5.6 Sol smoke test classified a synthetic interview request correctly with medium reasoning and 99% confidence. It performed no Gmail mutation or Telegram delivery.

## Model Guardian false-alert repair

- Ahmed approved the previously recommended repair: harden transient parsing, add regression coverage, retain one scheduler, and verify production behavior.
- Root cause confirmed: after `model-guardian-check.py` timed out and produced no status cache, `model-guardian-run.py` launched another expensive usage probe and converted its timeout/non-JSON output into a hard `DM_ALERT`.
- Added a typed transient-probe failure path, suppressed snapshot extraction when a transient check produced no cache, and prevented the redundant fallback usage probe. Provider/config/model errors remain hard-alert paths.
- Added two regression tests for timeout output without JSON and for the full no-cache transient path. The Model Guardian suite passes 6/6; Python compilation and diff checks pass.
- Retired only the duplicate root-cron Model Guardian command and its comment from the live crontab and `config/root-crontab.managed`. Preserved `model-guardian.timer` as the sole scheduler; it remains enabled and active. Backup: `config/root-crontab.backup-20260717T013552-before-model-guardian-dedupe.bak`.
- Production systemd run from 01:36:24 to 01:38:23 Cairo reproduced the timeout condition but exited successfully with `TRANSIENT_SUPPRESSED` and `NO_ALERTS`; no Telegram delivery occurred. Gateway connectivity remained healthy and the configured/reported model remained `openai/gpt-5.6-sol`.
- Residual risk: the OpenClaw CLI probes still peaked at 2,113,572,864 bytes and the timed-out run did not refresh quota data. Deduplication halves scheduled probe frequency, while deeper CLI memory/latency optimization remains separate work.

## Model Guardian quota-probe performance repair

- Ahmed approved fixing the remaining 2 GB probe-performance debt.
- Root cause: `openclaw status --usage --json` built the wider OpenClaw runtime status and loaded the CLI/plugin hook chain; `openclaw models status --plain` added a second heavyweight CLI load. Model Guardian only needed one Codex quota response plus two local model-config checks.
- Added `scripts/model-guardian-usage-probe.py`, a bounded quota-only HTTPS probe that reads the existing mode-0600 Codex OAuth file without printing credentials and emits only a normalized provider/quota snapshot. Replaced both heavyweight CLI checks with this probe and direct validation of `config/model-router.json` plus `/root/.openclaw/openclaw.json`.
- Added response-normalization, weekly-window, percentage-clamping, quota-only fallback, and transient-network regression coverage. Focused suite passes 13/13; forced invalid-proxy test preserved the `TRANSIENT:` marker; Memory Heist security suite passes 19/19.
- Live standalone benchmark: 1.69 seconds and 24,224 KiB peak RSS, returning the current Pro `168h` quota window. Previous production probes peaked around 2.0 GB and could run for minutes.
- Production systemd verification ran 02:34:09-02:34:12 Cairo, exited 0, peaked at 24,551,424 bytes, refreshed weekly quota to 94% remaining, reset transient failures to zero, and emitted `NO_ALERTS` with no Telegram attempt.
- `model-guardian.timer` remains the sole enabled/active scheduler. Gateway PID 648415 stayed active with zero restarts and TCP connectivity on 127.0.0.1:18789. Model remained `openai/gpt-5.6-sol`; no gateway restart, model change, config write, or timer change occurred.
- Rollback archive: `/root/.openclaw/backups/model-guardian-perf-20260717T021712.tar.gz`.
- Structured autoreview could not inspect the credential-reading helper because its secret scanner rejected source containing the literal `access_token`, even in an isolated staged worktree. This was a guardrail false positive, not a detected secret leak. Manual review found and fixed one transient-marker propagation bug before production; all focused tests and live checks were rerun afterward.

## Autoreview credential-field false-positive repair

- Ahmed approved repairing the autoreview preflight false positive that rejected `tokens.get('access_token')` in the Model Guardian quota helper.
- Root cause: credential field names were accepted only for a small hard-coded set of result objects, so the same harmless field-key lookup failed on ordinary mappings.
- Autoreview now recognizes the existing credential-field allowlist as safe when it is the first argument to any named `.get(...)` lookup. Hardcoded fallback arguments, token-shaped values, password-like literals, and unrelated secret-like lookup strings remain blocked.
- The original Model Guardian helper now passes autoreview dry-run preflight. Focused regression and negative controls pass; the deterministic suite passes 27/27. The full hardening suite passed 217/219 correctness checks, with only two time-ceiling failures: the relevant chained-receiver benchmark passed when rerun alone, while an unrelated C# URI benchmark remained above its five-second host timing threshold and was left out of scope.
- Structured Codex review used `gpt-5.6-sol` with high reasoning on the frozen two-file diff and returned no findings, `patch is correct`, confidence 0.91.

## July 15-16 VPS disk-100 incident diagnosis

- The VPS filesystem did not reach 100% capacity. Historical evidence shows the root filesystem later peaked at 81% with about 19 GB free; obsolete backups were pruned and current usage is 72% with 28 GB free. There were no `ENOSPC` or `No space left` errors.
- The 100% reading was swap utilization: sysstat recorded the full 16 GB swap consumed at 23:50 on July 15 and again at 00:08 on July 16, while available RAM fell to roughly 100-196 MB. Heavy paging produced disk pressure, OOM kills, a journald watchdog timeout, and reboots at 00:10 and 00:27.
- Root cause was overlapping high-memory OpenClaw work amplified by the old cron watchdog: it repeatedly relaunched the retired briefing pipeline when `data/jobs-merged.json` was stale and automatically retried failed OpenClaw crons. The July 16 repair disabled both behaviors; the watchdog now reports only, and its 02:00 July 17 run showed disk 72%, RAM 63%, zero alerts, and no legacy pipeline launch.
- Current verification: no OOM events since the final July 16 reboot; root filesystem 72% with 28 GB available; current available RAM about 2.6 GB. Swap still contains about 2.4 GB of cold pages but is no longer exhausted.
- Residual prevention gap: no proactive swap/PSI circuit breaker or snapshot free-space preflight is installed. Recommended next hardening is to alert and suppress new heavy scheduled work on sustained memory pressure, stagger high-memory jobs, enforce per-service memory/concurrency ceilings, and abort snapshots unless the post-build reserve remains safe.

## OpenClaw hook-relay overload repair

- Ahmed explicitly approved a deep live diagnosis and the narrowest corrective action for the agent/Telegram response stalls.
- Root cause was reproduced: Codex native pre-tool relay processes (`openclaw` launcher plus `openclaw-hooks` worker) could outlive their 10-second contract during gateway pressure. The abandoned relays multiplied, pushed the gateway cgroup near 5 GB, raised the host load above 40, delayed the event loop, and triggered another Telegram polling stall.
- Terminated only the abandoned diagnostic relay/probe trees. Gateway memory fell to about 1.6 GB, load fell to about 1, HTTP latency returned to milliseconds, and no gateway restart was required.
- Added `scripts/openclaw-hook-relay-reaper.py` and enabled the hardened user service `openclaw-hook-relay-reaper.service`. It checks every five seconds and targets only `openclaw-hooks` workers older than 15 seconds inside `openclaw-gateway.service`, together with their validated `openclaw` launcher. Live hooks inside their budget and unrelated processes are untouched.
- Service verification: enabled, active, zero restarts after correction, about 8 MB memory, systemd exposure rating 4.3 `OK`.
- Full verification passed: runtime patch checker, config validation, gateway status, gateway probe (`ok=true`, `degraded=false`), 16 plugins loaded with zero errors, Telegram connected with no pending reconnect, Memory Heist runtime hook count 4, security suite 19/19, and configured/current model `openai/gpt-5.6-sol`.
- Three previously failing cron jobs remain enabled and staggered; their last errors predate this repair and will verify the guard on their next scheduled runs. One 35-day-old queued context-maintenance task remains as a non-running audit warning because the supported cancel command reports no cancellable child session. Database backup: `/root/.openclaw/backups/openclaw-state-before-stale-task-cancel-20260717T193401.sqlite`.
