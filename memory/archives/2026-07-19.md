# Daily Notes - 2026-07-19

## Gateway event-loop health repair

- Diagnosed the weekly critical as CPU-contention plus over-severe alert classification on a 2-vCPU host. Gateway/Telegram stayed online, but active LinkedIn browser and hook-worker bursts produced isolated >=1s event-loop delay samples.
- Applied persistent systemd CPU isolation without restarting the gateway: `openclaw-gateway.service` CPUWeight=1000 and `openclaw-linkedin-chrome.service` CPUWeight=50. Gateway remained on PID 1414010 with zero restarts and `/health` returned HTTP 200 in about 52ms during final verification.
- Updated `scripts/weekly-self-health-fast.py` so delay-only transient samples are Important while sustained CPU/utilization, severe multi-second delay, or a slow health response remains Critical. Classification tests passed 3/3.
- Full weekly rerun now reports zero critical findings, Telegram connected, default model `openai/gpt-5.6-sol`, and the existing 0-critical/2-warning security backlog.
- Restored the transient LinkedIn +30 worker from its preserved ledger after it recycled during resource-control work. Verified it resumed at 12/30 with the same continuation and blocked ledgers.

## CMO LinkedIn publisher credential repair

- Diagnosed `cmo-notion-linkedin-post` rc=1: the active OpenClaw config still contained the intentionally disabled Composio plugin entry, but its direct-backend `apiKey`, `consumerKey`, and LinkedIn connected-account fields had been removed during the July 18 config rewrite.
- Verified the last intact credential set through the read-only `LINKEDIN_GET_MY_INFO` endpoint, restored only those three fields with a validated config patch, kept the plugin disabled, and did not restart the gateway because the publisher reads the config file directly.
- Re-ran the exact cron wrapper successfully. The approved post "Transformation Offices Should Remove Friction" went live at `https://www.linkedin.com/feed/update/urn:li:share:7484502128145707008/`; Notion is `Posted`, the public caption and visual match, and there is exactly one success ledger row for the date.
- Regenerated `reports/cron-health-latest.md`; it now reports `Problems: 0` and `cmo-notion-linkedin-post: ok, rc=0`.

## LinkedIn +30 persistent-worker repair

- Reconciled the dedicated continuation ledger against submission proof, ATS >=82, full-time evidence, 15-50 KB CV quality, duplicates, and current hard role-fit guards. The Confidential Project Manager (EX big 10) application remains factually preserved but is excluded from the target, correcting the strict baseline from 12/30 to 11/30.
- Replaced the dated transient worker path with enabled `linkedin-plus30-campaign.service`: one supervisor lock, atomic child-stage checkpoints, 30-second heartbeat, three-minute systemd watchdog, durable 0600 logs/state, recovery holds for interrupted candidates, and argument-safe status/restart handling.
- Two no-submission canaries passed 2/2 with unchanged ledger, blocked-ledger, and cursor hashes. Production then started active/running with zero restarts, one supervisor/runner pair, fresh heartbeat/watchdog, and matching 11/30 counters.
- The first production start exposed a `209/STDOUT` ordering issue because systemd opens append targets before `ExecStartPre`. Provisioned the log/state directories before installation, removed the ineffective pre-start line, refreshed the unit, and verified the live service stayed healthy.
- Rollback snapshot: `/root/.openclaw/workspace-hr/backups/linkedin-plus30-reliability-20260719T1300/`. Canonical runbook: `docs/solutions/jobs/linkedin-campaign-transient-worker.md`.
- A later manual TERM stopped the new service while `Director of Operations - Fashion` was checkpointed after CV verification. Live LinkedIn proof showed `Easy Apply`, not submitted; the candidate stayed uncounted and on recovery hold. Added a targeted fashion-retail operations mismatch guard before restarting.

## Canonical NASR writing standard

- Created `docs/standards/nasr-writing-standard.md` as the single voice system for direct replies, technical work, executive material, alerts, and public drafts.
- Replaced duplicated style prohibitions in `SOUL.md` and `USER.md` with references to the canonical standard, and added the same execution reference to `AGENTS.md`.
- Kept no-em-dash and anti-AI-tell rules as secondary guardrails. The primary system now emphasizes answer-first structure, plain language, evidence, judgment, natural rhythm, output profiles, examples, and a final quality check.
- Backed up the three core instruction files under `backups/core-instructions/2026-07-19-2321-writing-standard/` before editing and verified the changed files for broken references, trailing whitespace, and style regressions.
