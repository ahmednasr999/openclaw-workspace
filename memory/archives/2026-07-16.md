# 2026-07-16

## Cron watchdog legacy-loop repair

- Removed the watchdog's automatic relaunch of the retired briefing jobs pipeline when `data/jobs-merged.json` is stale; JobZoom and job-radar remain authoritative.
- Disabled automatic reruns of failed OpenClaw crons from the watchdog. Cron health is now report-only, with a 30-second bounded status check.
- Preserved the scheduled two-hour watchdog itself. No gateway restart was required.
- Live wrapper verification at 01:28 Cairo: return code 0 in 3 seconds, no legacy job-source processes spawned, RAM 2.1/7.8 GiB used with 5.7 GiB available.
- Residual warning: disk usage remains at 77%; unrelated to the repaired RAM/relaunch loop.

## Memory Heist guard promotion

- Promoted the reviewed GPT-Red pilot repair to `memory-heist-guard` version 1.0.1 after Ahmed's explicit approval.
- Closed nested snippet/metadata URL provenance confusion and generic-array wrapper smuggling while preserving the active SearXNG `results[]` contract.
- Final isolated Codex autoreview was clean (`patch is correct`, confidence 0.94).
- Gateway restart completed at 01:13:59 EEST, changing PID 17071 to 93238; connectivity and plugin loading verified.
- Post-restart combined plugin/pilot suite passed 19/19; runtime patch checker passed.
- Activation report and rollback bundle: `/root/.openclaw/backups/memory-heist-guard-promotion-20260716-004613/`.
- Current rollback archive SHA-256 at closeout: `f7a83bedec1381a5a2924dbfc79ec6c4bf841e558a8abcbaba011b8707d6a325`.
- Residual observability issue: plugin registry reports `hookCount: 0` despite fresh startup activation logs and passing hook-contract tests; treat as registry reporting uncertainty unless behavioral evidence contradicts it.

## Memory Heist restart gate and boundary hardening

- Installed an executable, exact-count Memory Heist security gate at `scripts/check-memory-heist-security-suite.py`; its source contract passes exactly 19/19 and is now required by the central OpenClaw update guard.
- Staged `memory-heist-guard` version 1.0.2 with fail-closed run-ID agreement, state revocation on conflicts, `agent_end` cleanup, and exact-URL Markdown suffix handling. The running gateway remains on 1.0.1; no restart occurred.
- Expanded the held-out GPT-Red boundary pilot: candidate results are 8/8 for cross-tool provenance and 5/5 for stale/cross-run isolation; the five top-level boundary tests pass.
- Added a fatal systemd pre-start contract gate. The next gateway restart is intentionally blocked until Ahmed explicitly approves `plugins.entries.memory-heist-guard.hooks.allowConversationAccess=true`, which enables the non-bundled plugin's `agent_end` hook.
- Browser click destinations remain unobservable at the plugin hook boundary and require a host-layer control; ordinary browser clicks were not disabled.
- Runtime health remained good without intervention: gateway probe healthy, Telegram connected, no plugin errors, and event-loop delay not degraded.

## Memory Heist Guard v1.0.2 activation

- Ahmed approved the final activation window.
- Enabled `plugins.entries.memory-heist-guard.hooks.allowConversationAccess=true`, allowing the non-bundled plugin's `agent_end` cleanup hook.
- Completed one controlled full-process gateway restart at 04:01 EEST; PID changed from 93238 to 251196.
- Systemd pre-start gates passed: runtime patches OK and the mandatory Memory Heist suite exactly 19/19.
- Independent post-restart checks passed: 19/19 security suite, four-hook live runtime contract with no diagnostics, and an expanded 24/24 plugin/boundary test run.
- Live hooks: `message_received`, `before_tool_call`, `after_tool_call`, and `agent_end`; runtime version/source are v1.0.2 from the workspace plugin.
- Gateway and event loop are healthy, Telegram is connected and passed deep status, and the post-restart direct-chat outbound/inbound path worked.
- GPT-5.6 Sol/high and the deterministic model-router default remained unchanged.
- Rollback bundle and activation report: `/root/.openclaw/backups/memory-heist-v102-activation-20260716-035826/`.
- Remaining limitation: browser-click destination protection is still deferred because resolved click URLs are not exposed to plugin hooks.

## Outcome-contract workflow

- Standardized substantial work around an internal task contract covering outcome, constraints, definition of done, evidence, authority, stop condition, owner, and review tier.
- Kept routine requests conversational: NASR infers the contract and asks only at material-risk or approval boundaries.
- Added proportional review rules: deterministic checks for routine work, contract-based self-review for substantial internal work, and fresh-context independent review for high-risk or reputation-sensitive deliverables.
- Independent reviewers remain read-only and do not expand permission or external-action authority; when fresh context is unavailable, a separated adversarial pass must be disclosed as non-independent.
- No model, runtime, permission, or external-action setting changed. Backup: `backups/20260716T043721-outcome-contract/`.

## LinkedIn +30 supervisor cadence

- Ahmed changed the July 15 LinkedIn +30 campaign supervisor retry cadence from every 15 minutes to every 12 hours while Ahmed-Mac is unavailable.
- Recreated the canonical `linkedin-plus30-20260715.service` with `--interval-seconds 43200`; the campaign ledger remained at 11/30 verified with 19 remaining.
- Updated the supervisor's default interval to 43,200 seconds so future recreation preserves the approved cadence.

## LinkedIn Comment Radar Mac-CDP recovery

- Root cause: after the 15:36 gateway restart, the browser route could not reach Ahmed-Mac and the CLI silently started a blank VPS Chrome profile. The 15:00 run first timed out after 1,500 seconds, then returned zero candidates.
- Repaired the radar to use a short-lived SSH tunnel to Ahmed-Mac's authenticated Chrome CDP directly. If the Mac session is unavailable, the workflow now stops instead of starting a blank VPS browser.
- Restored card-specific LinkedIn permalinks by reading `updateUrn` from each selected post menu's own Report link. The workflow does not click Copy link, Report, Like, Comment, Repost, Follow, or publish anything.
- Disabled Tavily/Exa fallback for daily LinkedIn comments; live Ahmed-Mac LinkedIn is now the only source.
- Production rerun at 16:33 produced 17 candidates, 8 approval-ready, and a full selected pack of 5. Verified the first permalink against the live LinkedIn post and matching author.
- Telegram delivery verified in gateway logs: summary message `17348` and approval cards `17349` through `17353` in CMO topic 7. No LinkedIn action was performed.
- Fixed the direct cron wrapper to treat a non-empty Telegram message ID as delivery proof even if CLI teardown times out, preventing false failure status and unsafe retries.
- VPS disk at closeout: 78 GB used of 96 GB, 19 GB available, 81% utilization. No destructive cleanup was performed.

## Approved backup cleanup

- Ahmed explicitly approved pruning superseded OpenClaw backups after the disk reached 81% utilization.
- Preserved `/root/openclaw-snapshot-20260715.tar.zst` as the latest recovery point. Its creation log records consistent online backups of 67 SQLite databases, successful `zstd -t`, required-path validation, and verified promotion with mode `600`.
- Removed all superseded contents under `/root/.openclaw/backups/`, including the now-obsolete Memory Heist activation rollback bundle referenced above. Removed 9,034,347,314 bytes; the backup directory remains present and empty.
- Disk utilization fell from 81% to 72%, with available space increasing from about 19 GB to 28 GB.
- Post-cleanup checks passed: OpenClaw configuration valid, gateway running and connectivity probe healthy, event loop not degraded, Telegram connected, and no plugin errors. No restart or configuration change was performed.

## HR Career Sentinel v1 built, disabled

- Built an isolated Gmail Pub/Sub career-attention workflow at `scripts/hr-career-sentinel.py`; it reads complete Gmail threads, uses GPT-5.6 Sol medium/high reasoning, and can only deliver structured Telegram alerts to Ahmed.
- Default behavior is silent. Deterministic vetoes cover generic job alerts, newsletters/marketing, LinkedIn/social digests, application acknowledgements, routine pre-interview rejections, unrelated mail, and threads Ahmed already answered.
- Deduplication uses Gmail message ID, thread ID, and normalized material event state in isolated SQLite storage. Pub/Sub is acknowledged only after durable local intake; ambiguous Telegram success is held without automatic retry.
- Final focused suite passed 17/17; the existing Gmail Pub/Sub ledger regression, systemd unit validation, source safety scan, plan validation, and live read-only dry-run passed. A live synthetic gateway check correctly classified an interview availability request with medium reasoning.
- The new unit was not installed, enabled, or started. `hr-career-sentinel.service` remained not-found/inactive and legacy `gog-gmail-watch.service` remained disabled/inactive.
