# Daily Notes - 2026-05-17

## OpenClaw Model Guardian / Health Guard Recovery

- Around 12:00 Cairo, Health Guard/Model Guardian produced alerts that Ahmed approved fixing.
- Diagnosis: not a GPT-5.5 or model-router failure. The guard was paging on slow OpenClaw CLI status/model probes under live load, then the notification path compounded it with a short Telegram message-send timeout.
- Local recovery changed the Model Guardian path to use longer bounded probe timeouts, cache the expensive status JSON during a run, suppress only the first transient timeout-only probe failure, and keep immediate alerts for config/model/provider errors.
- Files touched during the recovery included `scripts/model-guardian-check.py`, `scripts/model-guardian-run.py`, `scripts/check-openclaw-runtime-patches.py`, `memory/agent-traces/lessons.md`, and trace/learning logs. Backup files were created beside the edited scripts.
- Verification evidence from the closeout: Model Guardian dry-run no longer produced a false alert; Health Dashboard reported overall OK with gateway, config, runtime patches, context, logs, and dashboard service healthy; Ahmed was notified on Telegram with the concise closeout.

## Memory Flush Rule Reinforced

- During pre-compaction memory flushes, append only to the canonical daily file `memory/YYYY-MM-DD.md`. Do not create timestamped variants such as `YYYY-MM-DD-HHMM.md`, and keep bootstrap/reference files such as `MEMORY.md`, `DREAMS.md`, `SOUL.md`, `TOOLS.md`, and `AGENTS.md` read-only during the flush.

## Lossless-Claw 0.10.0 Update

- Ahmed approved continuing until completion. `lossless-claw` was updated to `0.10.0`, the gateway was restarted cleanly, LCM recall tools were verified, and the Telegram closeout was sent.
- Report: `/root/.openclaw/workspace/reports/lossless-claw-v0100-update-20260516-233212.txt`
- Backup: `/root/.openclaw/backups/lossless-claw-v0100-20260516-232807`
- Follow-up context: the health dashboard warning at that point was context-size hygiene, not a gateway/update failure.

## Telegram Command Repair Follow-up

- Telegram messages were arriving, but native Telegram command registration had been disabled in live config. The repair restored command registration and verified the command list after gateway restart.
- Ahmed reported `/new` still did nothing when typed inside a sentence. Follow-up finding: OpenClaw had not received a standalone `/new` message after restart, only the sentence containing `/new`; exact standalone command testing was the next diagnostic step.

## Model Guardian Provider Abort Fix

- At 18:03 Cairo, Model Guardian alerted on `FAIL: openai-codex provider error: This operation was aborted`.
- Diagnosis: GPT-5.5 and OpenAI Codex were healthy; the usage probe eventually returned valid provider JSON. The guard was treating a transient provider abort as a hard provider failure.
- Fix: updated `scripts/model-guardian-check.py` and `scripts/model-guardian-run.py` so `operation was aborted` provider errors are treated like bounded transient probe failures, and one run cannot double-count the same transient across the check and usage snapshot phases.
- Recovery: force-compacted the active DM LCM conversation from 130,309 context tokens to 11,238 tokens so Health Guard returned OK.
- Verification: direct `model-guardian-check.py` returned `ALL_OK`; direct `model-guardian-run.py` returned `NO_ALERTS`; `model-guardian.service` completed successfully; Health Dashboard returned overall OK.
