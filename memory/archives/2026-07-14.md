# 2026-07-14

- Hardened `scripts/daily-snapshot.sh` after a transient SQLite shared-memory file caused GNU tar to exit 1. The job now discovers included live SQLite databases (including migration-suffixed stores), creates and validates online `.backup` copies, excludes database originals plus WAL/SHM/journal sidecars, overlays the consistent copies at their original archive paths, and verifies every staged database member before atomic promotion.
- Production verification completed successfully: 65 SQLite databases backed up; `/root/openclaw-snapshot-20260714.tar.zst` promoted at 5.3 GB with zstd integrity, required restore paths, zero sidecars, zero staging artifacts, and exactly one copy of the critical LCM, flow, task, and memory databases. Latest-only retention remains active.
- Closed a follow-on false cron alarm caused by that successful verification having bypassed `cron-run-with-alert.sh`, leaving the prior failed wrapper status in place. Re-ran the hardened snapshot through the official wrapper: 66 SQLite online backups passed, the verified 5.4 GB archive was promoted, and `daily-snapshot` recorded `rc=0` at 09:54 Cairo. Updated `cron-health-report.py` so detected downstream problems remain report data and trigger its detailed alert without falsely marking the reporting job itself failed or sending a duplicate wrapper alert. The 09:55 rerun reported zero problems and both wrapper statuses green.
- Operationalized the NASR Engineering Loop inside `agent-ops-loops`: deterministic `/spec -> /build <-> /review -> approval -> merge` states, isolated work, two review mandates, bounded repairs, untrusted-ticket handling, and approval bound to the exact tested SHA. Added a fail-closed JSON record validator, template, 10 focused validator tests, and an isolated Git fixture pilot. The pilot reached `ready_for_approval`; five fixture tests, compile/security checks, and all negative guard probes passed. It correctly rejected emoji-only approval, stale-SHA approval, duplicate events, builder self-review, and failed-test progression. No merge, external write, Linear dependency, runtime change, or model switch occurred.
- Upgraded the live OpenClaw installation from `2026.7.1-beta.6` to `2026.7.1` stable and changed the update channel to stable. Reinstalled the gateway service metadata, aligned the secondary NVM CLI from `2026.7.1-beta.2` to stable, and verified config, gateway/probe, Telegram connectivity, 15 loaded plugins with zero errors, cron, GPT-5.6 Sol routing, runtime patches, and clean post-restart service logs. The rollback set and SQLite online backups are under `/root/.openclaw/backups/openclaw-update-20260714-111403/`.
- Closed the remaining session-watchdog observation after Ahmed approved follow-through. The single medium CMO/HR lane finding self-cleared on the immediate follow-up, and a second forced direct-command cron run at 20:36-20:39 Cairo passed with exit 0 and no output. The strict detector reported zero running or interrupted CMO/HR lanes, the gateway remained healthy and not degraded with Telegram connected, and the 21:55 scheduled run remains active. No further repair or restart was needed.
- Diagnosed a missed Sprinklr email alert. The Gmail Pub/Sub watcher received the event at 14:32:51 and the classifier processed UID 364923 at 14:32:58 as a high-priority Sprinklr interview/recruiter signal. The worker suppressed it because the formatted result was not urgent or action-required, then later polling runs misleadingly reported zero new messages after the UID checkpoint had advanced. Recorded the required policy change in `memory/lessons-learned.md`; no live watcher code or service lifecycle change was made in this diagnostic turn.
- Repaired and deployed the Gmail hiring-alert path with Ahmed's approval. The formatter now separates `action_required` from `hiring_process_update` using structured importance, action, pipeline, and email-key fields; misleading `all clear` wording was removed. The watcher now persists pending Telegram alerts before delivery, requires `ok=true` plus `messageId`, deduplicates, retries with bounded backoff, and reconciles the latest classified summary after restart or a registration gap. The exact missed Sprinklr case and synthetic reschedule, cancellation, feedback, rejection, action-request, noise, medium-priority pipeline, batching, receipt, failure, and recovery cases passed; core email tests passed 59/59. Restarted only `gog-gmail-watch.service`; it returned healthy with zero pending alerts and the gateway remained healthy and unchanged. Ahmed explicitly reaffirmed that no email may ever be sent or replied to without his clear approval; the watcher remains Telegram-alert-only and has no email-send path.
