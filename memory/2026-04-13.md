# 2026-04-13

- Investigated JobZoom zero-result tracks (Track 1 CDO/DT, Track 2 COO/Ops, Track 4 Industry-Specific).
- Root cause: `scripts/jobs-source-linkedin-jobspy.py` spent the full 150-search LinkedIn budget mostly on PMO/delivery titles, so zeroes in Tracks 1/2/4 were largely search-design artifacts, not evidence the market was dry.
- Rebalanced LinkedIn `PRIORITY_TITLES` and `KEYWORD_SEARCHES` to query DT/CDO, COO/Ops, PMO, and healthcare/fintech industry lanes directly while keeping total searches at 150.
- Verified script syntax with `python3 -m py_compile` and confirmed expected search budget still totals 150.
# 2026-04-13

- Investigated JobZoom zero-result tracks (Track 1 CDO/DT, Track 2 COO/Ops, Track 4 Industry-Specific).
- Root cause: `scripts/jobs-source-linkedin-jobspy.py` spent the full 150-search LinkedIn budget mostly on PMO/delivery titles, so zeroes in Tracks 1/2/4 were largely search-design artifacts, not evidence the market was dry.
- Rebalanced LinkedIn `PRIORITY_TITLES` and `KEYWORD_SEARCHES` to query DT/CDO, COO/Ops, PMO, and healthcare/fintech industry lanes directly while keeping total searches at 150.
- Verified script syntax with `python3 -m py_compile` and confirmed expected search budget still totals 150.

### OpenClaw update triage
- Evaluated live update risk on `/root/openclaw`; local branch was divergent from upstream and overlapped with upstream in doctor/status/subagent-spawn areas, so the recommendation was no blind live update.
- Created fallback branch `checkpoint/pre-update-2026-04-13` and saved restorable artifacts at `/root/.openclaw/workspace/artifacts/openclaw-pre-update-checkpoint-2026-04-13.patch` and `/root/.openclaw/workspace/artifacts/openclaw-pre-update-checkpoint-2026-04-13.status.txt` after repo hooks killed a checkpoint commit attempt.
- Built a narrow promotion branch `update/v2026.4.12-core-fixes` at commit `4b579c2e47`, carrying only the `sessions_spawn` explicit `cwd` fix and doctor memory-disabled guards; targeted tests passed, 65/65.
- Verified the runtime path is real cutover territory: gateway runs `/root/openclaw/dist/index.js` from the `/root/openclaw` repo, not just `/usr/bin/openclaw` as a standalone binary.
- Current cutover state at compaction: live checkout switched to `update/v2026.4.12-core-fixes`, deps synced, focused tests passed, and `dist` rebuilt; gateway restart was still pending.

### Coding governance and repo audit tooling
- Added canonical coding guidance files: `docs/agent-governance/NASR-Coding-Rules-v1.md` and `docs/agent-governance/NASR-ACP-Coding-Brief.md`, and trimmed the `coding-agent` skill to point at the canonical rules instead of duplicating doctrine.
- Added local read-only audit utility `scripts/skill-audit.py` with docs at `docs/agent-governance/skill-audit.md` to scan third-party skills/small repos for likely secrets, suspicious network writes, dangerous shell commands, and persistence hooks.
- Verified `skill-audit.py` on `/root/openclaw/skills/healthcheck` and `/root/.openclaw/workspace/docs/agent-governance`; both returned zero findings.
