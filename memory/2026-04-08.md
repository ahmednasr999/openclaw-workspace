# Daily Notes - 2026-04-08

# Daily Notes - 2026-04-08

### Search stack
- Local SearXNG was deployed under `services/searxng/`, bound to `127.0.0.1:8090`, with JSON enabled, limiter config added, and noisy/failing engines disabled.
- Shared router `skills/tavily-search/scripts/search.mjs` now uses Tavily from `config/tavily.json` first, then falls back to local SearXNG.
- `skills/tavily-search/scripts/extract.mjs` was aligned to local Tavily config loading.
- New research entrypoint `skills/tavily-search/scripts/research-search.mjs` uses Exa via Composio first, then falls back to Composio Search Web; direct Exa currently fails for credits exhaustion (`NO_MORE_CREDITS`).

### OpenClaw memory/runtime
- The install was already on OpenClaw `2026.4.8`; version alone did not enable all new benefits automatically.
- Memory stack was repaired by enabling `memory-core`, enabling `memory-wiki` in isolated mode, moving memory search off missing Ollama, and increasing QMD probe timeout.
- Wiki vault is ready at `~/.openclaw/wiki/main` and main workspace memory now reports `Vector: ready`.
- HR, CTO, and CMO workspaces still show `0 vectors`; they need separate indexing/rebuild work.

### Durable ops notes
- `HEARTBEAT.md` currently contains comments only, so heartbeat polls should return `HEARTBEAT_OK` unless tasks are added there.
- Multiple live secrets were exposed in chat/config inspection today, including Tavily, SearXNG, gateway, Slack, Telegram, and Composio-related values; treat them as compromised and rotate when practical.
- Separate from memory, a Slack/config read stack-trace issue still needs cleanup after the memory work.

### Memory follow-up completion
- HR, CTO, and CMO QMD stores were re-embedded directly with `qmd embed -f` against each agent cache; all three now report vectors present.
- The remaining false `vector unavailable` state was traced to an OpenClaw code cap that forced QMD status probes down to 5s regardless of configured timeout.
- Packaged OpenClaw runtime was patched so QMD probe timeout now honors configured `memory.qmd.limits.timeoutMs`, and all four agents now show `Vector: ready` in `openclaw memory status`.
- Packaged OpenClaw config loader was also patched defensively so a brittle Slack doctor-contract getter no longer breaks CLI config reads during doctor/memory commands.
